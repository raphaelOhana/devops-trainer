import type { Exercise } from '../engine/types';

// Lot « performance » — exercices experts générés (multi-agent), ancrés sur des
// sources de référence. Options équilibrées, bonne réponse en index 0
// (position randomisée par shuffle.ts).
export const performanceExercises: Exercise[] = [
  {"id":"perf-latency-numbers-ssd-aleatoire-vs-memoire","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Latency numbers : SSD aléatoire vs mémoire principale","company":"Jeff Dean, « Latency Numbers Every Programmer Should Know » (chiffres 2012, mis à jour) ; Brendan Gregg, Systems Performance 2e éd., ch. 2","question":"En ordre de grandeur, une lecture aléatoire de 4 Ko sur un SSD NVMe (~150 µs) est environ combien de fois plus lente qu'une référence à la mémoire principale (~100 ns) ?","options":["Environ 1 500 fois plus lente","Environ 150 fois plus lente","Environ 15 000 fois plus lente","Environ 30 fois plus lente"],"answer":0,"explanation":"150 µs = 150 000 ns et une référence mémoire ≈ 100 ns, donc 150 000 / 100 = 1 500. C'est l'ordre de grandeur clé : le SSD est ~1 500× la mémoire, mais reste ~100× plus rapide qu'un seek de disque rotatif (~10 ms). Le distracteur « 150 » confond µs et le facteur direct (oubli du rapport ns↔µs). « 15 000 » surestime d'un facteur 10 (confusion 4K vs 1 MB, ou 1 ms au lieu de 150 µs). « 30 » correspondrait au rapport mémoire↔L1 (100 ns / ~3 ns), pas au SSD. Retenir ces ordres de grandeur évite d'optimiser du CPU quand le vrai coût est une I/O 1000× plus chère.","tags":["latency-numbers","ssd","memoire","io-bound"],"optionNotes":["150 µs = 150 000 ns, ÷100 ns ≈ 1 500 : c'est le bon ordre de grandeur.","Oublie le rapport µs→ns et confond 150 µs avec un facteur 150 direct.","Surestime ×10 (confusion avec ~1 ms ou un bloc de 1 Mo au lieu de 150 µs).","30× correspond au rapport mémoire↔L1, pas au SSD."]},
  {"id":"perf-amdahl-gain-maximal-avec-5-de-code-seque","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Amdahl : gain maximal avec 5 % de code séquentiel","company":"Amdahl's Law ; Brendan Gregg, Systems Performance 2e éd., ch. 2 (scalability models)","question":"Un programme a 5 % de temps intrinsèquement séquentiel et 95 % parfaitement parallélisable. Quel est le speedup MAXIMAL théorique quel que soit le nombre de cœurs ?","options":["20×","95×","19×","5×"],"answer":0,"explanation":"Loi d'Amdahl : speedup max = 1 / fraction_séquentielle quand N→∞. Ici 1 / 0,05 = 20×. La partie parallèle tend vers 0 mais les 5 % séquentiels restent, plafonnant à 20×. « 95× » confond avec le pourcentage parallélisable. « 19× » est le speedup avec exactement 20 cœurs (1 / (0,05 + 0,95/20) = 1/0,0975 ≈ 10,3 en fait — piège arithmétique), pas la limite. « 5× » inverse le ratio (1/0,20). La leçon : au-delà d'un certain point, ajouter des cœurs ne rapporte rien ; il faut réduire la fraction séquentielle elle-même (verrous, barrières, I/O sérialisée).","tags":["amdahl","concurrence","scalability","parallelisme"],"optionNotes":["Speedup max = 1/fraction séquentielle = 1/0,05 = 20× quand N→∞.","Confond avec le pourcentage parallélisable (95 %), pas la limite.","19× serait un speedup à N fini, pas la borne théorique (N→∞).","Inverse le ratio : calcule 1/0,20 au lieu de 1/0,05."]},
  {"id":"perf-concatenation-dans-une-boucle-python","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Concaténation dans une boucle Python","company":"Jon Bentley, Programming Pearls ch. 8 ; CPython : str est immuable (PEP 8 / doc time complexity)","language":"python","code":"def build_csv(rows):\n    out = \"\"\n    for row in rows:\n        line = \",\".join(str(c) for c in row)\n        out += line + \"\\n\"\n    return out\n\n# rows : 1_000_000 lignes","question":"Pourquoi cette fonction devient-elle catastrophiquement lente sur 1 million de lignes ?","options":["`out += ...` recrée une nouvelle chaîne à chaque itération, copiant tout l'accumulateur : coût global O(n²) sur la taille totale","`\",\".join(...)` sur un générateur est O(n²) car join doit d'abord matérialiser puis re-parcourir le générateur deux fois","`str(c)` déclenche un appel système par cellule, saturant le GIL et sérialisant toute la boucle","L'opérateur `+` entre `line` et `\"\\n\"` force une réallocation du tas à chaque tour, fragmentant la mémoire de façon quadratique"],"answer":0,"explanation":"Les chaînes Python sont immuables : `out += line` alloue une NOUVELLE chaîne et copie l'intégralité de `out` déjà accumulé. Après k lignes, out a une taille proportionnelle à k, donc la i-ème concaténation coûte O(i) et la somme donne O(n²) sur la longueur totale — désastreux à 1M lignes. La correction : accumuler dans une `list` et faire un seul `\"\".join(parts)` à la fin (O(n)), ou écrire directement dans un fichier/StringIO. Note : CPython a une optimisation opportuniste pour `s += ` quand le refcount est 1, mais elle est fragile et non garantie. Le distracteur sur `join` est faux (join est O(total)). Pas d'appel système par `str(c)`. Le `+` local sur `line` est négligeable (petites chaînes).","tags":["complexite","string","python","quadratique"],"optionNotes":["str est immuable : chaque += copie tout l'accumulateur déjà bâti, total O(n²).","Faux : join parcourt le générateur une fois, coût O(total) linéaire.","Faux : str(c) est un appel Python pur, aucun syscall ni blocage GIL.","Faux : le + sur de petites chaînes locales est négligeable, pas quadratique."]},
  {"id":"perf-aos-vs-soa-pour-un-traitement-vectorisab","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"AoS vs SoA pour un traitement vectorisable","company":"Brendan Gregg, Systems Performance 2e éd., ch. 6 (CPU caches) ; Agner Fog, Optimizing software in C++ (data layout)","question":"Vous avez 10 M de particules {x, y, z, vx, vy, vz, mass, id} et une boucle chaude qui ne met à jour QUE x += vx pour toutes. Pourquoi le layout SoA (tableaux séparés x[], vx[]) écrase-t-il l'AoS (tableau de structs) ici ?","options":["En AoS, chaque ligne de cache de 64 o chargée pour lire un x contient aussi y,z,vy,mass,id inutilisés : le débit utile chute et la vectorisation SIMD ne peut pas charger des x contigus","En AoS, le compilateur insère du padding d'alignement entre chaque struct, doublant l'empreinte mémoire et provoquant systématiquement des TLB misses à chaque accès","En SoA, les tableaux séparés tiennent chacun en cache L1 alors que l'AoS ne tient jamais en L1, ce qui supprime tous les défauts de cache de dernier niveau","En AoS, le prefetcher matériel est désactivé car il ne détecte pas de motif d'accès régulier sur des structures de taille variable"],"answer":0,"explanation":"Le point central est l'efficacité de la ligne de cache et la vectorisation. En AoS, la struct fait ~56 o : lire un seul champ x amène toute la ligne de 64 o, dont ~48 o inutiles pour ce noyau → gaspillage de bande passante mémoire et impossibilité de charger 8 x consécutifs dans un registre AVX. En SoA, x[] est parfaitement contigu : chaque ligne de cache est 100 % utile et le compilateur auto-vectorise x[i]+=vx[i]. Distracteurs : le padding existe mais n'explique pas le facteur principal ; les tableaux SoA de 10M floats (40 Mo) ne tiennent pas non plus en L1 (le gain est la contiguïté, pas « tout en L1 ») ; le prefetcher matériel fonctionne très bien sur des structs de taille fixe. C'est un cas d'école du 'pointer/field chasing' vs données contiguës.","tags":["cache","soa-aos","simd","data-layout"],"optionNotes":["Lire un x charge 64 o dont ~48 inutiles ; SoA rend x[] contigu et vectorisable.","Le padding existe mais n'explique pas le facteur principal (gaspillage de ligne).","Faux : 10M floats (40 Mo) ne tiennent pas en L1 ; le gain est la contiguïté.","Faux : le prefetcher fonctionne très bien sur des structs de taille fixe."]},
  {"id":"perf-faux-partage-entre-threads-en-c","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Faux partage entre threads en C","company":"Brendan Gregg, Systems Performance 2e éd., ch. 6 ; Intel Optimization Manual (false sharing) ; CWE-N/A (perf)","language":"c","code":"struct counters { long a; long b; };\nstruct counters c;\n\nvoid* worker_a(void* _) {\n    for (long i = 0; i < 1000000000; i++) c.a++;\n    return NULL;\n}\nvoid* worker_b(void* _) {\n    for (long i = 0; i < 1000000000; i++) c.b++;\n    return NULL;\n}\n// c.a incrementé par le thread A, c.b par le thread B, en parallele","question":"Les deux compteurs sont indépendants, pourtant la version 2 threads est plus lente que séquentielle. Quelle est la cause exacte ?","options":["`a` et `b` partagent la même ligne de cache de 64 o : chaque écriture invalide la ligne dans le cache de l'autre cœur (protocole MESI), provoquant un ping-pong coûteux — c'est du faux partage","L'accès concurrent à la même struct crée une data race sur la struct entière, obligeant le CPU à sérialiser les écritures via un verrou mémoire implicite","Le compilateur ne peut pas garder `c.a` dans un registre car `c` est global, forçant un aller-retour mémoire à chaque ++ sur les deux threads","Les deux threads se disputent le même contrôleur mémoire, saturant la bande passante DRAM bien avant la fin des boucles"],"answer":0,"explanation":"C'est le cas canonique du faux partage : `a` et `b` sont adjacents dans la même ligne de cache de 64 octets. Bien qu'aucune donnée ne soit logiquement partagée, le protocole de cohérence MESI travaille à la granularité de la LIGNE : quand le cœur A écrit `a`, il invalide la ligne dans le cache du cœur B (qui y détient `b`), et réciproquement. Résultat : la ligne fait un ping-pong entre les deux caches L1/L2, chaque ++ coûtant un rapatriement inter-cœurs (~40-100 ns) au lieu d'un hit L1 (~1 ns). Correction : aligner/rembourrer chaque compteur sur sa propre ligne (`alignas(64)` ou padding). Ce n'est PAS une data race (accès à des champs distincts), pas un problème de registre (le vrai coût est la cohérence), et la bande passante DRAM n'est pas saturée (les données tiennent en cache — le coût est la cohérence, pas le débit).","tags":["false-sharing","cache-line","concurrence","mesi"],"optionNotes":["a et b partagent une ligne de 64 o : chaque écriture l'invalide chez l'autre cœur (MESI).","Pas une data race : les champs sont distincts, aucun verrou implicite en jeu.","Le coût n'est pas le registre mais la cohérence inter-cœurs de la ligne.","Les données tiennent en cache : c'est la cohérence, pas la bande passante DRAM."]},
  {"id":"perf-profiling-par-echantillonnage-vs-instrum","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Profiling par échantillonnage vs instrumentation","company":"Brendan Gregg, Systems Performance 2e éd., ch. 5-6 (profiling, perf) ; Brendan Gregg, BPF Performance Tools","question":"Pourquoi un profileur par ÉCHANTILLONNAGE (perf record à 99 Hz) est-il souvent préféré à un profileur par INSTRUMENTATION pour trouver le hot path en production ?","options":["L'échantillonnage a un overhead quasi constant et faible (indépendant du nombre d'appels), sans biaiser les fonctions courtes très appelées, alors que l'instrumentation ajoute un coût par appel qui gonfle et déforme le profil","L'échantillonnage mesure le temps réel exact de chaque fonction tandis que l'instrumentation ne fait qu'estimer statistiquement, d'où une précision moindre sur les fonctions longues","L'instrumentation ne peut pas capturer les piles d'appels natives, alors que l'échantillonnage reconstruit toujours des stacks complètes sans coût supplémentaire de déroulage","L'échantillonnage évite tout effet d'observateur car il s'exécute sur un cœur dédié isolé, ce que l'instrumentation ne permet jamais de faire"],"answer":0,"explanation":"Clé : l'échantillonnage interrompt le programme à fréquence fixe (ex. 99 Hz) et lit la pile ; son overhead ne dépend PAS du nombre d'appels de fonction, il est faible et prévisible, et il ne pénalise pas spécifiquement les petites fonctions chaudes. L'instrumentation insère un prologue/épilogue de mesure à CHAQUE entrée/sortie de fonction : pour une fonction minuscule appelée des milliards de fois, le coût de mesure peut dépasser le coût réel, distordant complètement le profil (les fonctions courtes très appelées paraissent dominantes à cause de l'instrumentation elle-même). Distracteurs : l'échantillonnage donne des estimations statistiques, pas des temps exacts (inverse de l'option) ; le déroulage de pile a un coût et peut manquer sans frame pointers/DWARF ; il n'y a pas de cœur isolé magique. « Mesurer avant d'optimiser » exige justement un outil à faible biais.","tags":["profiling","sampling","perf","hot-path"],"optionNotes":["Overhead indépendant du nombre d'appels, faible et sans biais sur les fonctions courtes.","Inversé : l'échantillonnage donne une estimation statistique, pas un temps exact.","Faux : le déroulage de pile a un coût et peut échouer sans frame pointers/DWARF.","Faux : aucun cœur isolé magique ; l'échantillonnage interrompt bien le programme."]},
  {"id":"perf-recherche-lineaire-repetee-en-javascript","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Recherche linéaire répétée en JavaScript","company":"Jon Bentley, Programming Pearls ch. 8 ; MDN Array.prototype.includes (complexité O(n))","language":"javascript","code":"function dedupeAgainstBlocklist(items, blocklist) {\n  const result = [];\n  for (const item of items) {\n    if (!blocklist.includes(item.id)) {\n      result.push(item);\n    }\n  }\n  return result;\n}\n// items: 200_000 elements, blocklist: 50_000 ids","question":"Quelle est la complexité réelle et la correction adéquate ?","options":["O(n·m) car `blocklist.includes` est un scan O(m) exécuté n fois ; convertir la blocklist en `Set` une fois donne des lookups O(1) et ramène l'ensemble à O(n+m)","O(n log m) car `includes` sur un tableau utilise une recherche dichotomique interne ; il suffit de trier `items` au préalable pour atteindre O(n)","O(n) déjà optimal ; le vrai goulot est `result.push` qui réalloue le tableau à chaque insertion, à remplacer par une taille pré-allouée","O(n²) car la boucle `for...of` crée un itérateur par élément ; remplacer par une boucle indexée classique supprime le facteur quadratique"],"answer":0,"explanation":"`Array.prototype.includes` fait une recherche LINÉAIRE O(m). L'exécuter pour chacun des n items donne O(n·m) = 200 000 × 50 000 = 10 milliards de comparaisons. La correction canonique : `const bl = new Set(blocklist)` (construit en O(m)), puis `bl.has(item.id)` en O(1) amorti, total O(n+m). Distracteurs : `includes` sur Array n'est PAS dichotomique (pas de tri interne) ; `push` sur un tableau JS est amorti O(1) (redimensionnement géométrique) et n'est pas le goulot ; `for...of` a un léger surcoût d'itérateur mais reste O(n), pas quadratique. C'est l'exemple type du 'choisir la bonne structure de données' : Set/Map hachés contre scan linéaire.","tags":["complexite","set","javascript","hot-loop"],"optionNotes":["includes scanne O(m) répété n fois → O(n·m) ; un Set ramène les lookups à O(1).","Faux : includes sur Array est linéaire, pas une recherche dichotomique.","Faux : push est amorti O(1) (croissance géométrique), pas le goulot.","Faux : for...of a un léger surcoût d'itérateur mais reste O(n)."]},
  {"id":"perf-amortissement-du-tableau-dynamique-facte","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Amortissement du tableau dynamique : facteur 2 vs 1,1","company":"CLRS, Introduction to Algorithms ch. 17 (amortized analysis) ; doc std::vector growth","question":"Un tableau dynamique double sa capacité quand il est plein. Pourquoi le facteur de croissance 2 garantit-il un `push_back` en O(1) AMORTI, et quel est le compromis d'un facteur plus petit comme 1,1 ?","options":["Avec un facteur >1, le coût total de copie sur n insertions est une série géométrique bornée par O(n), donc O(1) amorti ; 1,1 réduit le gaspillage mémoire mais multiplie le nombre de réallocations/copies et empêche souvent la réutilisation des blocs libérés","Seul le facteur exactement 2 donne O(1) amorti ; tout autre facteur (1,1 comme 3) dégrade en O(log n) amorti car les tailles ne sont plus des puissances de deux alignées","Le facteur 1,1 est strictement supérieur : il donne aussi O(1) amorti tout en gaspillant moins de mémoire, sans aucun inconvénient de performance sur les copies","Un facteur 2 rend chaque push individuel O(1) dans le pire cas ; c'est le seul moyen d'éviter les pics de latence lors des réallocations"],"answer":0,"explanation":"L'analyse par agrégat/potentiel : avec un facteur de croissance g>1, les copies totales pour atteindre n éléments forment une série géométrique n·(1 + 1/g + 1/g² + …) bornée, soit O(n) au total, donc O(1) AMORTI par push (pas par push individuel : une réallocation isolée reste O(n) dans le pire cas — d'où le distracteur 4, faux). Le facteur n'a pas besoin d'être 2 : 1,5 (MSVC), 2 (libstdc++) marchent tous, réfutant le distracteur 2. Un facteur 1,1 donne bien O(1) amorti mais avec une constante horrible : beaucoup plus de réallocations et de copies, et il empêche l'allocateur de réutiliser les anciens blocs (avec g<2 ou g≤φ, la somme des blocs précédents peut suffire à contenir le suivant — argument classique en faveur d'un facteur <2 pour la réutilisation). Le distracteur 3 nie à tort le surcoût de copie.","tags":["amortissement","dynamic-array","complexite","memoire"],"optionNotes":["Copies totales = série géométrique bornée O(n), donc O(1) amorti ; 1,1 multiplie réallocs.","Faux : 1,5 ou 2 marchent aussi ; le facteur exact 2 n'est pas requis.","Faux : 1,1 donne bien O(1) amorti mais avec une constante énorme (bien plus de copies).","Faux : un push isolé lors d'une réallocation reste O(n) au pire cas."]},
  {"id":"perf-n-1-requetes-cachees-derriere-une-boucle","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"N+1 requêtes cachées derrière une boucle","company":"Brendan Gregg, Systems Performance 2e éd., ch. 2 (USE method, I/O) ; anti-pattern N+1 (Martin Fowler, PoEAA)","language":"python","code":"def enrich_orders(order_ids, db):\n    orders = []\n    for oid in order_ids:  # 5000 ids\n        order = db.query_one(\"SELECT * FROM orders WHERE id = %s\", (oid,))\n        order.customer = db.query_one(\n            \"SELECT * FROM customers WHERE id = %s\", (order.customer_id,))\n        orders.append(order)\n    return orders","question":"Cette fonction sature le réseau/DB alors que le CPU est inactif. Quel est le diagnostic et la meilleure correction ?","options":["Problème I/O-bound de type N+1 : ~10 000 allers-retours réseau séquentiels, chacun payant la latence RTT (~0,5 ms) ; batcher via `WHERE id IN (...)` ou une jointure réduit à 1-2 requêtes","Problème CPU-bound : le parsing des lignes SQL en objets Python domine ; il faut paralléliser la boucle avec un ThreadPool pour saturer les cœurs","Fuite mémoire : chaque `query_one` garde un curseur ouvert ; fermer explicitement le curseur après chaque appel résout la lenteur","Contention de verrou côté DB : les SELECT posent des verrous partagés qui se bloquent mutuellement ; passer en READ UNCOMMITTED supprime l'attente"],"answer":0,"explanation":"C'est l'anti-pattern N+1 classique, purement I/O-bound. Pour 5000 ids on émet 5000 requêtes 'orders' + 5000 'customers' = 10 000 allers-retours SÉQUENTIELS. Même si chaque requête est triviale côté DB, on paie 10 000 × latence réseau RTT (souvent 0,3-1 ms intra-datacenter, cf. ~500 µs Jeff Dean), soit plusieurs secondes dominées par l'attente, CPU quasi nul. La bonne correction : batcher — `SELECT * FROM orders WHERE id IN (...)` puis un seul `SELECT * FROM customers WHERE id IN (...)`, ou une jointure, ramenant à 1-2 aller-retours. Distracteurs : ce n'est pas CPU-bound (le CPU est inactif — paralléliser des threads masquerait un peu la latence mais ne supprime pas le fond du problème et ajoute de la charge DB) ; ni fuite mémoire ni verrous SHARED (les SELECT ne se bloquent pas en MVCC). Reconnaître I/O-bound vs CPU-bound est le préalable à toute optimisation.","tags":["n+1","io-bound","batching","sql"],"optionNotes":["N+1 I/O-bound : 10 000 allers-retours séquentiels ; batcher via IN(...) réduit à 1-2 requêtes.","Faux : le CPU est inactif, ce n'est pas le parsing mais l'attente réseau.","Faux : pas de fuite mémoire ; le problème est le nombre d'allers-retours.","Faux : les SELECT ne se bloquent pas en MVCC ; aucun verrou en cause."]},
  {"id":"perf-optimisation-prematuree-la-citation-exac","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Optimisation prématurée : la citation exacte de Knuth","company":"Donald Knuth, « Structured Programming with go to Statements », Computing Surveys 1974 ; repris par Bentley, Programming Pearls","question":"La maxime « premature optimization is the root of all evil » est souvent citée pour justifier de ne jamais optimiser. Que dit réellement le contexte complet de Knuth ?","options":["Il faut ignorer les petites optimisations ~97 % du temps, mais ne pas rater les 3 % critiques ; un bon programmeur doit identifier ce code critique par la mesure et l'optimiser, pas fuir toute optimisation","Toute optimisation avant que le code ne soit entièrement écrit et testé est nuisible ; Knuth recommande d'optimiser uniquement après la livraison en production","Les micro-optimisations manuelles sont toujours contre-productives car les compilateurs modernes battent systématiquement l'humain ; il faut se reposer sur le compilateur","L'optimisation prématurée désigne le choix d'algorithmes complexes trop tôt ; Knuth conseille de toujours commencer par l'algorithme le plus simple même s'il est asymptotiquement pire"],"answer":0,"explanation":"La citation est presque toujours tronquée. Knuth écrit : « We should forget about small efficiencies, say about 97% of the time: premature optimization is the root of all evil. Yet we should not pass up our opportunities in that critical 3%. » Le message est nuancé : renoncer aux MICRO-optimisations dispersées, mais traquer et optimiser le code critique — identifié par la MESURE (profiling). Ce n'est pas un permis de ne jamais optimiser. Distracteurs : Knuth ne dit pas d'attendre la production, ni que le compilateur bat toujours l'humain (il défendait même le go-to pour la perf des boucles chaudes !), ni que c'est une question de complexité algorithmique — c'est bien une question d'effort dépensé au mauvais endroit vs le hot path. La discipline : mesurer, localiser les 3 %, agir là.","tags":["premature-optimization","knuth","profiling","philosophie"],"optionNotes":["Knuth : renoncer aux micro-optim ~97 % du temps, mais traquer les 3 % critiques par la mesure.","Faux : Knuth ne dit pas d'attendre la production pour optimiser.","Faux : Knuth défendait le go-to pour les boucles chaudes ; le compilateur ne gagne pas toujours.","Faux : il s'agit de l'effort dépensé au mauvais endroit, pas de complexité algorithmique."]},
  {"id":"perf-boxing-dans-une-boucle-chaude-java","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Boxing dans une boucle chaude Java","company":"Brendan Gregg, Systems Performance 2e éd., ch. 5 (GC) ; Java Language Spec 5.1.7 (autoboxing) ; Effective Java, Item 61","language":"java","code":"long sumOfEvens(int[] data) {\n    Long total = 0L;\n    for (int i = 0; i < data.length; i++) {\n        if (data[i] % 2 == 0) {\n            total += data[i];\n        }\n    }\n    return total;\n}","question":"Cette méthode génère une pression GC énorme sur de grands tableaux. Pourquoi ?","options":["`total` est un `Long` (objet) : chaque `total += data[i]` auto-unbox, additionne, puis auto-BOX le résultat en un NOUVEL objet Long sur le tas, créant des millions d'objets éphémères qui saturent l'espace Eden","L'opération `data[i] % 2` alloue un objet Integer intermédiaire à chaque test de parité, doublant le nombre d'allocations par rapport à la somme elle-même","Le `int[]` est copié dans un buffer temporaire à chaque accès `data[i]` car les tableaux Java ne sont pas accessibles directement depuis une boucle for indexée","L'auto-widening de `int` vers `long` force une boîte englobante `Number` intermédiaire qui reste référencée jusqu'à la fin de la méthode, empêchant toute collecte"],"answer":0,"explanation":"Le type déclaré `Long` (avec majuscule, wrapper objet) est le piège. Comme `Long` est immuable, `total += data[i]` se compile en : unbox de total → addition primitive → BOXING du résultat en un nouvel objet `Long.valueOf(...)`. Pour un grand tableau, cela crée des millions de Long éphémères alloués sur l'Eden, déclenchant des cycles GC minor incessants et détruisant le débit. La correction triviale : déclarer `long total = 0L;` (primitif) → zéro allocation. Distracteurs : `data[i] % 2` sur des primitifs `int` n'alloue rien ; l'accès `data[i]` ne copie pas le tableau ; le widening int→long est une opération primitive sans boxe. C'est l'illustration parfaite de la 'pression mémoire / GC' due à un choix de type wrapper au lieu de primitif dans un hot path.","tags":["autoboxing","gc","java","hot-loop"],"optionNotes":["total est un Long : chaque += auto-boxe le résultat, créant des millions d'objets sur l'Eden.","Faux : data[i] % 2 opère sur des int primitifs, aucune allocation.","Faux : l'accès data[i] ne copie pas le tableau.","Faux : le widening int→long est une opération primitive, sans boxing."]},
  {"id":"perf-contention-de-verrou-granularite-fine-vs","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Contention de verrou : granularité fine vs grosse","company":"Brendan Gregg, Systems Performance 2e éd., ch. 5 ; Java Concurrency in Practice, ch. 11 (lock granularity)","question":"Un cache concurrent protégé par UN verrou global montre 80 % du temps passé en attente de ce verrou sous forte charge. Vous envisagez le lock striping (N verrous, un par bucket de hash). Quel est le vrai compromis ?","options":["Le striping réduit la contention en répartissant les accès sur N verrous indépendants, mais complique les opérations globales atomiques (taille exacte, resize) qui doivent désormais acquérir tous les verrous, et augmente l'empreinte mémoire","Le striping élimine totalement la contention car chaque thread obtient son propre verrou dédié, sans aucun coût additionnel sur les opérations de lecture ou d'écriture","Le striping est inutile ici : il faut d'abord passer le verrou global en spinlock, car les spinlocks n'ont jamais de contention contrairement aux mutex bloquants","Le striping dégrade toujours la performance car maintenir N verrous multiplie par N le coût de chaque acquisition, même en l'absence de contention"],"answer":0,"explanation":"Le lock striping (utilisé historiquement par `ConcurrentHashMap` avant Java 8) partitionne les données en segments, chacun avec son propre verrou, dérivé du hash de la clé : deux threads touchant des buckets différents ne se disputent plus rien → contention effondrée. MAIS le compromis réel : les opérations GLOBALES (taille exacte, rehash/resize, itération cohérente) doivent acquérir TOUS les verrous, deviennent coûteuses/délicates, et l'empreinte mémoire augmente (N objets verrou + risque de moins bonne localité). Distracteurs : la contention n'est pas 'totalement éliminée' (deux clés peuvent tomber dans le même stripe) ; un spinlock ne supprime pas la contention (il la transforme en busy-wait qui brûle du CPU) ; sans contention, N verrous ne coûtent pas N× (chaque opération n'en prend qu'un). La granularité fine attaque la contention au prix de la complexité des invariants globaux.","tags":["lock-striping","contention","concurrence","granularite"],"optionNotes":["Le striping répartit sur N verrous mais rend les ops globales (taille, resize) coûteuses.","Faux : deux clés peuvent tomber dans le même stripe ; contention non nulle.","Faux : un spinlock ne supprime pas la contention, il la transforme en busy-wait.","Faux : sans contention, chaque opération ne prend qu'un verrou, pas N."]},
  {"id":"perf-fonction-sur-colonne-indexee-en-sql","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Fonction sur colonne indexée en SQL","company":"Use The Index, Luke! (M. Winand) ch. « Functions » (SARGability) ; doc PostgreSQL index usage","language":"sql","code":"-- index existant : CREATE INDEX idx_orders_created ON orders(created_at);\nSELECT id, total\nFROM orders\nWHERE DATE(created_at) = '2026-08-21'\n  AND status = 'shipped';","question":"Malgré l'index sur created_at, le plan fait un Seq Scan complet. Pourquoi, et comment rendre la requête SARGable ?","options":["Envelopper la colonne dans `DATE(created_at)` rend le prédicat non-SARGable : l'index B-tree porte sur `created_at` brut, pas sur `DATE(created_at)`, donc il ne peut être utilisé ; réécrire en `created_at >= '2026-08-21' AND created_at < '2026-08-22'`","L'index n'est pas utilisé car `status = 'shipped'` n'est pas couvert par idx_orders_created ; il faut d'abord ajouter status à l'index avant que created_at soit exploitable","`DATE()` est déterministe donc le planificateur devrait l'utiliser ; le Seq Scan vient de statistiques périmées, un simple `ANALYZE orders` corrige le plan sans changer la requête","La comparaison à un littéral chaîne `'2026-08-21'` force une conversion implicite de type qui invalide l'index ; caster explicitement en `::date` des deux côtés restaure l'index"],"answer":0,"explanation":"Appliquer une fonction à la colonne indexée (`DATE(created_at)`) détruit la SARGabilité : l'index B-tree ordonne les valeurs BRUTES de `created_at`, pas les valeurs transformées `DATE(created_at)`. Le planificateur ne peut donc pas faire de range scan et retombe sur un Seq Scan (à moins d'un index fonctionnel sur `DATE(created_at)`). La correction canonique : garder la colonne nue et transformer le LITTÉRAL en un intervalle : `created_at >= '2026-08-21' AND created_at < '2026-08-22'` → range scan direct sur l'index. Distracteurs : ajouter status ne débloque pas created_at (le problème est la fonction, pas la couverture) ; ANALYZE corrige les cardinalités mais pas un prédicat non-SARGable ; il n'y a pas de conversion de type invalidante ici (le vrai coupable est la fonction sur la colonne). Règle : ne jamais envelopper la colonne indexée dans une fonction côté prédicat.","tags":["sql","index","sargable","seq-scan"],"optionNotes":["DATE(created_at) casse la SARGabilité ; réécrire en intervalle rend l'index utilisable.","Faux : ajouter status ne débloque pas created_at ; le souci est la fonction.","Faux : ANALYZE corrige les stats mais pas un prédicat non-SARGable.","Faux : pas de conversion de type ici ; le coupable est la fonction sur la colonne."]},
  {"id":"perf-benchmark-jvm-fausse-par-le-jit","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Benchmark JVM faussé par le JIT","company":"JMH (Java Microbenchmark Harness) doc ; Brendan Gregg, Systems Performance 2e éd., ch. 5 ; Aleksey Shipilëv, « JMH: The Lesser of Two Evils »","question":"Un micro-benchmark maison mesure une méthode en la lançant une fois et en chronométrant avec System.nanoTime(). Le résultat est 20× plus lent que la réalité en production. Quelle est la cause la plus probable ?","options":["Absence de warmup : la JVM exécute d'abord en interprété puis compile en C1/C2 après un seuil d'appels ; mesurer avant que le JIT n'ait compilé et optimisé la méthode capture le code froid non optimisé","System.nanoTime() a une résolution de 20 ms sur la JVM, donc toute mesure sous cette granularité est arrondie vers le haut d'un facteur constant qu'il faut soustraire","La première exécution alloue le class loader et le JIT compile immédiatement en C2 avant le premier appel, ajoutant un coût de compilation fixe qui domine une exécution unique","Le GC s'exécute obligatoirement avant chaque mesure isolée pour libérer l'espace, et son temps est comptabilisé dans le chronomètre, gonflant systématiquement le résultat"],"answer":0,"explanation":"Le piège n°1 du micro-benchmarking JVM est l'absence de WARMUP. La JVM démarre en mode interprété ; ce n'est qu'après un seuil d'invocations/boucles (compteurs, ~10 000 par défaut) que le JIT (C1 puis C2) compile et optimise la méthode (inlining, escape analysis, vectorisation). Chronométrer la toute première exécution mesure le code interprété froid — d'où un facteur 10-20× plus lent que le code chaud de production. C'est pourquoi JMH impose des itérations de warmup avant les itérations de mesure. Distracteurs : `nanoTime()` a une résolution bien meilleure que 20 ms (souvent dizaines de ns) ; le JIT ne compile PAS en C2 avant le premier appel (c'est justement l'inverse) ; le GC ne tourne pas systématiquement avant chaque mesure. Un benchmark correct exige warmup, plusieurs itérations, prise en compte de la variance, et neutralisation du dead-code elimination.","tags":["benchmarking","jit","warmup","jvm"],"optionNotes":["Sans warmup, on mesure le code interprété froid avant que le JIT ne compile (C1/C2).","Faux : nanoTime a une résolution de l'ordre de la dizaine de ns, pas 20 ms.","Faux : le JIT ne compile pas en C2 avant le premier appel ; c'est l'inverse.","Faux : le GC ne tourne pas systématiquement avant chaque mesure."]},
  {"id":"perf-test-d-appartenance-sur-une-liste-python","type":"find-error","domain":"software","topic":"performance","difficulty":"intermediate","title":"Test d'appartenance sur une liste Python","company":"Python docs — TimeComplexity wiki (list vs set) ; Jon Bentley, Programming Pearls ch. 8","language":"python","code":"def count_new_visitors(events, known_users):\n    new = 0\n    for e in events:            # 1_000_000 events\n        if e.user_id not in known_users:   # known_users est une list\n            new += 1\n    return new","question":"`known_users` contient 100 000 identifiants sous forme de liste. Quel est le problème de performance ?","options":["`x not in liste` fait une recherche linéaire O(m) ; répétée sur n événements cela donne O(n·m) = 10^11 opérations ; utiliser un `set` rend le test O(1) amorti","L'itération `for e in events` matérialise tout le générateur en mémoire d'abord, causant un swap disque qui ralentit chaque test d'appartenance","`new += 1` sur un int provoque une réallocation d'objet à chaque incrément car les int Python sont immuables, ce qui domine le temps total","Le test `not in` construit un itérateur inverse sur la liste à chaque appel, doublant le coût par rapport à un `in` direct"],"answer":0,"explanation":"Sur une `list`, l'opérateur `in`/`not in` fait un SCAN LINÉAIRE O(m) (il compare élément par élément). Exécuté pour chacun des n=1M événements sur une liste de m=100k, cela donne O(n·m) ≈ 10^11 comparaisons — plusieurs minutes. La correction : `known_users = set(known_users)` (une fois, O(m)), puis `in` sur un set est O(1) amorti (table de hachage). Total O(n+m). Distracteurs : `for e in events` ne matérialise rien de plus qu'un itérateur ; `new += 1` sur de petits int réutilise le cache d'entiers CPython et est négligeable ; `not in` ne construit pas d'itérateur inverse. C'est le cas d'école 'choisir la bonne structure' : set/dict haché vs liste pour l'appartenance.","tags":["set","complexite","python","membership"],"optionNotes":["in sur une liste scanne O(m) ; répété sur n events → O(n·m) ; un set rend le test O(1).","Faux : for e ne matérialise qu'un itérateur, aucun swap disque.","Faux : new += 1 sur de petits int réutilise le cache d'entiers, négligeable.","Faux : not in ne construit aucun itérateur inverse."]},
  {"id":"perf-compare-and-swap-et-le-probleme-aba","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Compare-and-swap et le problème ABA","company":"Maurice Herlihy & Nir Shavit, The Art of Multiprocessor Programming, ch. 10-11 (lock-free) ; CWE-N/A","question":"Une pile lock-free basée sur CAS sur le pointeur de tête peut souffrir du problème ABA. En quoi consiste-t-il exactement ?","options":["Un thread lit la tête A, est suspendu ; d'autres threads pop A, pop B, puis re-push A (réutilisant l'adresse) ; au réveil, le CAS du premier thread voit toujours A et réussit à tort, corrompant la pile alors que la structure a changé entre-temps","Deux threads exécutent simultanément un CAS sur la même tête A ; comme les deux comparent à A, les deux réussissent et poussent leur valeur, écrasant l'un des deux nœuds silencieusement","Un thread lit A, un autre change A en B ; le CAS échoue correctement mais le thread relit B, refait son calcul, et boucle indéfiniment car A oscille entre deux valeurs sous forte contention","Le CAS compare la valeur mais pas le type ; si l'adresse A est réinterprétée comme un entier B par un autre thread, la comparaison bit-à-bit réussit alors que les données pointées sont incompatibles"],"answer":0,"explanation":"Le problème ABA : un thread T1 lit head = A (et mémorise A->next). Avant que T1 ne fasse son CAS, d'autres threads pop A, pop d'autres nœuds, puis re-push un nœud RÉUTILISANT l'adresse A (allocateurs recyclant la mémoire). La tête vaut de nouveau A, mais A->next a changé. Le CAS(head, A, A->next_mémorisé) de T1 COMPARE la valeur A et RÉUSSIT — alors que la structure sous-jacente est différente — restaurant un ->next périmé et corrompant la pile (nœuds perdus ou cycle). CAS ne détecte que l'égalité de valeur, pas l'historique. Solutions : pointeurs marqués/compteurs de version (tagged pointers, double-width CAS), hazard pointers, ou epoch-based reclamation. Distracteurs : deux CAS simultanés ne réussissent PAS tous les deux (CAS est atomique, un seul gagne) ; l'option 3 décrit un simple retry légitime, pas ABA ; l'option 4 invente un problème de typage inexistant.","tags":["lock-free","cas","aba","concurrence"],"optionNotes":["Adresse A recyclée : le CAS voit toujours A et réussit alors que la structure a changé.","Faux : le CAS est atomique, un seul thread gagne, pas les deux simultanément.","Faux : ceci décrit un retry légitime, pas le problème ABA.","Faux : invente un problème de typage ; ABA est un recyclage d'adresse."]},
  {"id":"perf-regex-recompilee-dans-la-boucle","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Regex recompilée dans la boucle","company":"Brendan Gregg, Systems Performance 2e éd., ch. 5 ; Python re doc (compile caching) ; Jon Bentley, Programming Pearls","language":"python","code":"import re\n\ndef extract_ids(lines):\n    results = []\n    for line in lines:                       # 5_000_000 lignes\n        m = re.search(r\"id=(\\d{6,12})&token=[a-f0-9]{32}\", line)\n        if m:\n            results.append(m.group(1))\n    return results","question":"Sur 5 millions de lignes, où est le coût évitable et quelle est la nuance CPython ?","options":["Compiler le pattern à chaque itération est le coût évitable ; sortir `pat = re.compile(...)` hors boucle l'élimine. CPython met en cache les patterns compilés (~512 entrées) donc l'impact est modéré ici, mais un cache miss ou re.purge le rendrait dramatique","Le vrai coût est `results.append` qui déclenche un GC générationnel à chaque insertion ; pré-allouer la liste supprime les pauses","`re.search` scanne toute la ligne alors que `re.match` suffirait ; comme le motif n'est pas ancré, chaque appel est O(longueur²) à cause du backtracking exponentiel garanti","La classe `[a-f0-9]{32}` provoque un catastrophic backtracking systématique ; il faut la remplacer par un quantificateur possessif pour éviter l'explosion exponentielle"],"answer":0,"explanation":"Le coût structurel évitable est la RE-COMPILATION du motif à chaque itération. La bonne pratique est de compiler UNE fois hors boucle : `pat = re.compile(r\"...\"); pat.search(line)`. La nuance CPython importante : le module `re` maintient un cache interne des derniers patterns compilés (par défaut ~512 en CPython 3), donc `re.search(literal, ...)` en boucle ne recompile pas réellement à chaque fois — l'impact est atténué. Mais ce cache est petit, vidé par `re.purge()`, et inefficace si le pattern est construit dynamiquement (clé de cache différente) : la version compilée-hors-boucle est robuste et évite même le lookup de cache. Distracteurs : `append` est amorti O(1), pas de GC par insertion ; ce motif n'a pas de backtracking catastrophique (les classes `\\d{6,12}` et `[a-f0-9]{32}` sont bornées et déterministes, pas d'alternance ambiguë imbriquée) ; les quantificateurs possessifs ne sont pas le sujet ici.","tags":["regex","hot-loop","python","caching"],"optionNotes":["Recompiler le motif à chaque tour est évitable ; compiler hors boucle (cache CPython atténue).","Faux : append est amorti O(1), pas de GC par insertion.","Faux : ce motif borné n'a pas de backtracking catastrophique ; search n'est pas O(n²).","Faux : les classes bornées {32}/{6,12} sont déterministes, pas d'explosion."]},
  {"id":"perf-parcours-de-liste-chainee-vs-tableau-le-","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Parcours de liste chaînée vs tableau : le pointer chasing","company":"Brendan Gregg, Systems Performance 2e éd., ch. 6 (memory, prefetch) ; « What Every Programmer Should Know About Memory », U. Drepper","question":"Sommer 10 M d'entiers stockés dans une liste chaînée est souvent 5 à 20× plus lent que dans un tableau contigu, alors que les deux sont O(n). Quelle est la raison dominante ?","options":["Le pointer chasing : chaque nœud est à une adresse arbitraire non prédictible, donc chaque `node = node->next` provoque un défaut de cache (~100 ns en mémoire) que le prefetcher matériel ne peut anticiper, alors que le tableau est parcouru en accès séquentiel parfaitement préchargé","La liste chaînée stocke un pointeur de 8 o par nœud en plus de la valeur, doublant les données à lire ; le tableau étant deux fois plus petit en mémoire, il est mécaniquement deux fois plus rapide à sommer","L'addition sur une liste chaînée empêche la vectorisation SIMD car les valeurs sont de type pointeur et non entier, forçant une conversion scalaire coûteuse à chaque nœud","Le tableau bénéficie du fait que sa taille est connue à la compilation, ce qui permet au compilateur de dérouler entièrement la boucle, alors que la liste chaînée impose une boucle dynamique non déroulable"],"answer":0,"explanation":"La cause dominante est le POINTER CHASING et son effet sur le cache. Dans une liste chaînée, les nœuds sont typiquement dispersés à des adresses arbitraires (surtout après des allocations/insertions successives) : suivre `node->next` est une dépendance de données sérielle vers une adresse imprévisible, que le prefetcher matériel (qui détecte des motifs de stride réguliers) ne peut PAS anticiper. Chaque accès risque un cache miss coûtant ~100 ns (mémoire principale), soit des centaines de cycles gaspillés par nœud. Le tableau contigu, lui, est parcouru en stride constant : le prefetcher amène les lignes à l'avance, chaque valeur est un hit L1, et le CPU peut vectoriser/pipeliner. Distracteurs : le surcoût de pointeur (8 o) n'explique pas un facteur 5-20× (au mieux 2×) ; la vectorisation n'est pas bloquée par un 'type pointeur' (les valeurs restent des entiers) ; le déroulage n'est pas la clé — c'est la latence mémoire non masquée. C'est LE cas d'école des données cache-friendly.","tags":["pointer-chasing","cache","linked-list","prefetch"],"optionNotes":["Chaque node->next saute à une adresse imprévisible → cache miss que le prefetcher ne devine pas.","Faux : le surcoût de 8 o explique au plus 2×, pas un facteur 5-20×.","Faux : les valeurs restent des entiers ; la vectorisation n'est pas bloquée par un type pointeur.","Faux : le déroulage n'est pas la clé ; c'est la latence mémoire non masquée."]},
  {"id":"perf-defaut-de-cache-memo-mutable-default-arg","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Défaut de cache mémo : mutable default argument","company":"Python docs — « Default parameter values are evaluated once » ; anti-pattern mutable default (Effective Python, Item 24)","language":"python","code":"def fib(n, memo={}):\n    if n < 2:\n        return n\n    if n not in memo:\n        memo[n] = fib(n - 1, memo) + fib(n - 2, memo)\n    return memo[n]","question":"Ce mémo fonctionne et est rapide, mais recèle un piège subtil de correction/performance à long terme. Lequel ?","options":["L'argument par défaut `memo={}` est évalué UNE seule fois à la définition et PARTAGÉ entre tous les appels : le cache grossit indéfiniment (fuite mémoire de facto) et devient un état global caché, dangereux si les résultats devaient dépendre d'un contexte","À chaque appel, un nouveau dictionnaire `{}` est créé, donc la mémoisation ne persiste jamais entre appels top-level : la complexité reste exponentielle O(2^n) malgré l'apparence de cache","Le passage récursif de `memo` copie le dictionnaire à chaque niveau de récursion, transformant l'espace mémoire en O(n²) au lieu de O(n)","L'accès `n not in memo` recalcule le hash de n à chaque niveau, ce qui rend chaque lookup O(n) et la fonction globalement O(n²)"],"answer":0,"explanation":"Le piège est le mutable default argument : `memo={}` est évalué UNE fois, au moment de la DÉFINITION de la fonction, et le MÊME dictionnaire est réutilisé à tous les appels ultérieurs qui n'en fournissent pas. Conséquences : (1) le cache persiste entre appels top-level indépendants — bon pour la vitesse ici, mais c'est un état global caché ; (2) il grossit sans limite (fuite mémoire de facto sur un long process) ; (3) c'est un bug latent si un jour le résultat devait dépendre d'un paramètre externe non capturé dans la clé. La pratique recommandée : `memo=None` puis `if memo is None: memo = {}`, ou `functools.lru_cache`. Distracteurs : un nouveau `{}` n'est PAS créé à chaque appel (tout le contresens de ce piège) ; le dict est passé par référence, pas copié (espace O(n)) ; le hash d'un int est O(1). La subtilité est la sémantique d'évaluation unique du défaut mutable.","tags":["memoization","python","cache-invalidation","state"],"optionNotes":["memo={} est évalué une fois à la définition et partagé : état global caché qui grossit sans fin.","Faux : justement aucun nouveau {} par appel ; la mémoisation persiste (c'est le piège).","Faux : le dict est passé par référence, pas copié ; espace O(n).","Faux : le hash d'un int est O(1), le lookup aussi."]},
  {"id":"perf-vectorisation-simd-empechee-par-une-depe","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Vectorisation SIMD empêchée par une dépendance","company":"Agner Fog, Optimizing software in C++ (loop dependencies) ; Intel Optimization Manual ; auto-vectorisation GCC/LLVM","question":"Le compilateur refuse d'auto-vectoriser cette boucle : `for(i=1;i<n;i++) a[i] = a[i-1] * 0.5 + b[i];`. Pourquoi la SIMD est-elle impossible telle quelle ?","options":["Dépendance portée par la boucle (loop-carried dependency) : `a[i]` dépend de `a[i-1]` calculé à l'itération précédente ; les lanes SIMD calculant en parallèle a[i], a[i+1]… ne disposent pas encore de la valeur amont, ce qui interdit l'exécution vectorielle directe","Le mélange de multiplication flottante et d'addition empêche l'usage d'une instruction FMA vectorielle, seul mode sous lequel le compilateur accepte de vectoriser des boucles flottantes","L'accès `b[i]` risque un aliasing avec `a[i]` : sans mot-clé restrict, le compilateur suppose que les deux tableaux se chevauchent, mais c'est le seul obstacle, levable par restrict","La constante `0.5` en double force une promotion de précision qui casse l'alignement des registres SIMD 256 bits, réductible en castant explicitement en float"],"answer":0,"explanation":"L'obstacle fondamental est une DÉPENDANCE PORTÉE PAR LA BOUCLE (loop-carried dependency) : `a[i]` utilise `a[i-1]`, qui vient d'être écrit à l'itération précédente. La vectorisation SIMD veut calculer plusieurs itérations SIMULTANÉMENT (a[i], a[i+1], a[i+2], a[i+3] dans un registre), mais a[i+1] a besoin de a[i] AVANT que le vecteur ne soit calculé — la donnée n'existe pas encore. C'est un récurrence scalaire (proche d'un IIR filter) qui n'est pas vectorisable naïvement (des techniques existent : réassociation, calcul de la récurrence par blocs, mais pas trivialement). Distracteurs : le mélange mul/add n'empêche rien (FMA est un bonus, pas une condition) ; l'aliasing a[]/b[] est un obstacle SÉPARÉ et levable par `restrict`, mais il n'est PAS le vrai bloqueur ici (la vraie barrière est la dépendance a[i]↔a[i-1], indépendante de b) ; la constante 0.5 n'a aucun effet d'alignement. Reconnaître les dépendances est le préalable à toute vectorisation.","tags":["simd","vectorisation","loop-dependency","compilateur"],"optionNotes":["a[i] dépend de a[i-1] : les lanes SIMD n'ont pas encore la valeur amont, vectorisation impossible.","Faux : mélanger mul et add n'empêche rien ; FMA est un bonus, pas une condition.","Faux : l'aliasing est un obstacle séparé, levable par restrict, pas le vrai bloqueur.","Faux : la constante 0,5 n'a aucun effet sur l'alignement des registres."]},
  {"id":"perf-defer-dans-une-boucle-go","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"defer dans une boucle Go","company":"Go doc — « Defer, Panic, and Recover » ; Effective Go ; Brendan Gregg, Systems Performance (resource leaks)","language":"go","code":"func processFiles(paths []string) error {\n    for _, p := range paths {   // 100_000 fichiers\n        f, err := os.Open(p)\n        if err != nil {\n            return err\n        }\n        defer f.Close()\n        data, _ := io.ReadAll(f)\n        handle(data)\n    }\n    return nil\n}","question":"Quel double problème ce code présente-t-il avec 100 000 fichiers ?","options":["Les `defer f.Close()` s'empilent et ne s'exécutent qu'au RETOUR de la fonction, pas à chaque itération : on garde 100 000 descripteurs de fichiers ouverts simultanément (épuisement des FD) et la pile de defers consomme de la mémoire jusqu'à la fin","`defer` dans une boucle crée une goroutine par itération qui ferme le fichier de façon asynchrone, provoquant une explosion du scheduler et une contention sur le runtime","`io.ReadAll` sans buffer lit octet par octet via un syscall par octet, ce qui domine totalement le temps ; le defer n'a qu'un impact négligeable","Chaque `defer` est réévalué à chaque tour de boucle et rappelle `f.Close()` sur tous les fichiers précédents, fermant plusieurs fois les mêmes descripteurs"],"answer":0,"explanation":"En Go, `defer` diffère l'exécution jusqu'au RETOUR DE LA FONCTION ENGLOBANTE, pas à la fin de l'itération de boucle. Dans cette boucle, chaque `defer f.Close()` est empilé mais AUCUN ne s'exécute avant le `return` final. Conséquences : (1) 100 000 descripteurs de fichiers restent OUVERTS simultanément → dépassement de la limite de FD du process (`too many open files`) ; (2) la pile de defers accumulée consomme de la mémoire. La correction : encapsuler le corps dans une fonction (closure appelée dans la boucle, où le defer se déclenche à chaque retour) ou fermer explicitement `f.Close()` avant la fin de l'itération (attention à ne pas fermer avant ReadAll). Distracteurs : `defer` ne lance PAS de goroutine ; `io.ReadAll` bufferise (pas un syscall/octet) ; les defers ne se rappellent pas cumulativement (chacun ferme son propre f capturé). Piège classique d'accumulation de ressources.","tags":["go","defer","resource-leak","file-descriptors"],"optionNotes":["defer s'exécute au return de la fonction, pas par itération : 100k FD restent ouverts.","Faux : defer ne lance aucune goroutine.","Faux : io.ReadAll bufferise ; ce n'est pas un syscall par octet.","Faux : chaque defer ferme son propre f capturé, pas cumulativement."]},
  {"id":"perf-prediction-de-branche-trie-vs-non-trie","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Prédiction de branche : trié vs non trié","company":"Brendan Gregg, Systems Performance 2e éd., ch. 6 ; question classique StackOverflow « Why is processing a sorted array faster » (branch prediction)","question":"Une boucle qui somme conditionnellement `if (data[i] >= 128) sum += data[i];` sur un tableau d'octets aléatoires est ~6× plus lente que sur le MÊME tableau trié. Pourquoi ?","options":["Sur données aléatoires, le prédicteur de branche se trompe ~50 % du temps ; chaque misprediction vide le pipeline (~15-20 cycles). Trié, la branche est presque toujours prise puis presque toujours non-prise : le prédicteur atteint ~100 % de réussite","Le tri place les grandes valeurs en fin de tableau, ce qui améliore la localité de cache car le CPU précharge séquentiellement les valeurs qui déclenchent effectivement l'addition","Sur données triées, le compilateur détecte la monotonie et remplace la branche par une addition sans condition à partir du seuil, éliminant complètement le test au runtime","Le tri aligne les octets ≥128 sur des frontières de mot machine, permettant au CPU d'utiliser une addition vectorielle que les données non triées interdisent"],"answer":0,"explanation":"C'est l'effet emblématique de la PRÉDICTION DE BRANCHE. Le CPU spécule sur le résultat du `if` pour garder son pipeline plein. Sur des octets ALÉATOIRES, la condition `>= 128` est vraie ~50 % du temps de façon imprévisible : le prédicteur échoue environ une fois sur deux, et chaque misprediction provoque un flush du pipeline (~15-20 cycles perdus), d'où l'effondrement du débit. Sur le MÊME tableau TRIÉ, la condition est fausse pour tout le début (valeurs <128) puis vraie pour toute la fin : un seul changement de comportement, le prédicteur converge vers ~100 % de succès → quasi zéro flush. Distracteurs : la localité de cache est identique (même tableau, même parcours séquentiel) ; le compilateur ne connaît pas l'ordre des données au runtime ; l'alignement/vectorisation n'est pas en jeu (le tri ne change pas l'alignement des octets). On peut d'ailleurs supprimer la branche entièrement avec une astuce branchless (masque), rendant les deux cas également rapides — preuve que la branche était la cause.","tags":["branch-prediction","pipeline","cache","cpu"],"optionNotes":["Données aléatoires : ~50 % de mispredictions, chacune vidant le pipeline (~15-20 cycles).","Faux : même tableau, même parcours séquentiel → localité de cache identique.","Faux : le compilateur ignore l'ordre des données au runtime.","Faux : le tri ne change pas l'alignement des octets ni n'active la vectorisation."]},
  {"id":"perf-count-reevalue-dans-la-condition-de-bouc","type":"find-error","domain":"software","topic":"performance","difficulty":"intermediate","title":"count() réévalué dans la condition de boucle PHP","company":"PHP manual — count() ; Jon Bentley, Programming Pearls ch. 8 (moving code out of loops)","language":"php","code":"$total = 0;\nfor ($i = 0; $i < count($items); $i++) {\n    for ($j = 0; $j < count($items); $j++) {\n        if ($items[$i]->cat === $items[$j]->cat) {\n            $total++;\n        }\n    }\n}","question":"Au-delà de la complexité O(n²) intrinsèque, quelle inefficacité supplémentaire ce code introduit-il ?","options":["`count($items)` est réévalué à CHAQUE test de condition des deux boucles ; hisser `$n = count($items)` hors des boucles évite n² (voire n³) appels à count et supprime un surcoût constant non négligeable","L'accès `$items[$i]->cat` crée une copie de l'objet à chaque itération car PHP passe les objets par valeur, ce qui domine le temps d'exécution","La comparaison `===` sur des chaînes force une comparaison octet par octet O(longueur) qui rend la boucle globalement O(n³), inévitable sans hachage","`$total++` déclenche une conversion de type entière à chaque incrément car PHP stocke les entiers en flottant, saturant l'unité FPU"],"answer":0,"explanation":"En PHP, `count($items)` dans la condition de boucle est ÉVALUÉ À CHAQUE ITÉRATION. Ici, la condition interne s'exécute ~n² fois et appelle count à chaque fois → jusqu'à n² appels à count (et la condition externe n fois). Bien que count() sur un array PHP soit O(1) (taille mémorisée), l'appel de fonction répété a un surcoût réel et évitable. La règle de Bentley 'sortir les invariants de la boucle' impose de calculer `$n = count($items);` une fois avant les boucles. (Le fond O(n²) de l'algorithme reste, et pourrait lui-même être remplacé par un comptage par catégorie via un tableau associatif ramenant à O(n).) Distracteurs : PHP passe les OBJETS par handle/référence, pas de copie sur `->cat` ; `===` sur des chaînes courtes n'entraîne pas un O(n³) dominant ; les entiers PHP ne sont pas des flottants. L'inefficacité additionnelle demandée est bien la réévaluation de count.","tags":["loop-invariant","php","complexite","count"],"optionNotes":["count($items) est réévalué à chaque test des deux boucles ; hisser $n évite n² appels.","Faux : PHP passe les objets par handle, pas de copie sur ->cat.","Faux : === sur des chaînes courtes ne domine pas en O(n³).","Faux : les entiers PHP ne sont pas stockés en flottant."]},
  {"id":"perf-lazy-vs-eager-evaluation-paresseuse-et-s","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Lazy vs eager : évaluation paresseuse et son coût caché","company":"Structure and Interpretation of Computer Programs, ch. 3.5 (streams) ; docs Java Stream / Python generators","question":"Passer une pipeline de transformation d'une liste eager (matérialisée) à un générateur/stream lazy réduit la mémoire, mais introduit parfois une régression de performance. Dans quel cas la version LAZY est-elle plus lente ?","options":["Quand la source est PARCOURUE PLUSIEURS FOIS : le lazy recalcule toute la chaîne à chaque parcours (aucun résultat stocké), alors que l'eager paie le calcul une fois et réutilise. Le lazy ajoute aussi un surcoût par élément (fermetures/appels d'itérateur) qui pénalise les petits jeux tenant en cache","Le lazy est toujours plus lent car il force un boxing de chaque élément intermédiaire pour transiter entre les étapes de la pipeline, contrairement à l'eager qui opère sur des primitifs","Le lazy dégrade la complexité asymptotique de O(n) à O(n log n) car chaque élément doit être inséré dans une file d'attente ordonnée avant d'être consommé par l'étape suivante","Le lazy empêche toute parallélisation alors que l'eager se parallélise automatiquement, ce qui explique systématiquement l'écart de performance sur machines multicœurs"],"answer":0,"explanation":"L'évaluation paresseuse ne stocke pas les résultats intermédiaires : elle recalcule à la demande. Deux cas où elle perd : (1) parcours MULTIPLES — un générateur/stream est souvent à usage unique ou recalcule toute la chaîne à chaque itération complète, alors qu'une liste eager matérialisée paie le coût UNE fois et se relit gratuitement ; (2) sur de PETITS jeux tenant déjà en cache, le surcoût par élément (indirections d'itérateur, fermetures, appels virtuels entre étapes) domine et rend le lazy plus lent que le batch eager vectorisable. Le lazy gagne quand : jeu énorme/infini, court-circuit précoce (find/any/limit), ou mémoire contrainte. Distracteurs : le lazy n'impose pas de boxing systématique (les streams primitifs existent, ex. IntStream) ; il ne change pas la complexité asymptotique en O(n log n) (pas de file ordonnée) ; le lazy n'interdit pas le parallélisme (parallelStream est lazy). Le vrai compromis est : mémoire/court-circuit vs recalcul et surcoût par élément.","tags":["lazy-eager","streams","memoire","iterateur"],"optionNotes":["Lazy recalcule toute la chaîne à chaque parcours et ajoute un surcoût par élément.","Faux : les streams primitifs (IntStream) évitent le boxing systématique.","Faux : le lazy ne dégrade pas la complexité en O(n log n) ; pas de file ordonnée.","Faux : parallelStream est lazy ; le lazy n'interdit pas le parallélisme."]},
  {"id":"perf-allocation-dans-la-boucle-chaude-rust","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Allocation dans la boucle chaude (Rust)","company":"The Rust Performance Book (allocations, clone) ; Brendan Gregg, Systems Performance ch. 5 (allocator)","language":"rust","code":"fn count_matches(haystacks: &[String], needle: &str) -> usize {\n    let mut count = 0;\n    for h in haystacks {                 // 10_000_000 elements\n        let lower = h.to_lowercase();\n        if lower.contains(&needle.to_lowercase()) {\n            count += 1;\n        }\n    }\n    count\n}","question":"Ce code alloue massivement sur le tas dans la boucle chaude. Quel est le principal coût évitable ?","options":["`needle.to_lowercase()` est recalculé et réalloue une String à CHAQUE itération alors qu'il est invariant : le hisser hors de la boucle supprime ~10 millions d'allocations inutiles (celle sur `h` reste, mais dépend de h)","`h.to_lowercase()` retourne une référence empruntée qui garde `h` verrouillé, empêchant le compilateur de libérer la mémoire et provoquant une accumulation linéaire jusqu'à la fin","`contains` construit un automate de Boyer-Moore à chaque appel, ce qui domine le temps ; utiliser `find` à la place réutilise l'automate entre itérations","`count += 1` sur un usize force une vérification de dépassement (overflow check) à chaque incrément en mode release, saturant l'ALU sur 10 millions d'itérations"],"answer":0,"explanation":"Le coût le plus flagrant et évitable : `needle.to_lowercase()` est INVARIANT de boucle mais recalculé à chaque tour, allouant une nouvelle String sur le tas ~10 millions de fois. La correction immédiate (règle de Bentley) : `let needle_lower = needle.to_lowercase();` AVANT la boucle. L'allocation `h.to_lowercase()`, elle, dépend de h et ne peut être simplement hissée — on l'optimiserait autrement (comparaison sans allocation via iterators de chars, ou réutilisation d'un buffer). Distracteurs : `to_lowercase()` retourne une `String` possédée (allocation), pas une référence empruntée — donc pas de 'verrou' ni d'accumulation ; `contains(&str)` ne construit pas un Boyer-Moore coûteux réutilisable (et `find` ne changerait rien) ; en mode release, les overflow checks arithmétiques sont désactivés par défaut (wrapping), donc `count += 1` est quasi gratuit. Le point clé est l'allocation répétée d'un invariant dans le hot path.","tags":["rust","allocation","loop-invariant","hot-loop"],"optionNotes":["needle.to_lowercase() est invariant mais réalloue à chaque tour ; le hisser supprime ~10M allocs.","Faux : to_lowercase() retourne une String possédée, pas une référence empruntée.","Faux : contains ne construit pas de Boyer-Moore réutilisable ; find ne changerait rien.","Faux : en release les overflow checks sont désactivés ; count += 1 est quasi gratuit."]},
  {"id":"perf-i-o-bound-pourquoi-async-bat-le-pool-de-","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"I/O-bound : pourquoi async bat le pool de threads","company":"Brendan Gregg, Systems Performance 2e éd., ch. 5 (threads, scheduling) ; modèle d'événements (epoll/kqueue)","question":"Pour un serveur gérant 50 000 connexions majoritairement en attente réseau (I/O-bound), pourquoi un modèle async/event-loop (epoll) surpasse-t-il « un thread OS par connexion » ?","options":["Chaque thread OS coûte ~1 Mo de pile + un contexte kernel ; 50 000 threads gaspillent des Go de mémoire et écroulent le scheduler en context-switches, alors qu'une event-loop multiplexe des milliers de sockets sur peu de threads sans bloquer sur l'attente","L'async exécute les requêtes réellement en parallèle sur tous les cœurs alors que les threads OS sont limités à un seul cœur par le GIL du kernel, ce qui plafonne le débit du modèle par thread","Les threads OS ne peuvent pas utiliser epoll ni les I/O non bloquantes, contrairement à l'event-loop ; c'est la seule façon d'éviter le polling actif qui brûle 100 % CPU par connexion","L'event-loop supprime la latence réseau elle-même en pré-établissant toutes les connexions à l'avance, alors que le modèle par thread doit rouvrir un socket à chaque requête"],"answer":0,"explanation":"Pour une charge I/O-bound massivement concurrente, le modèle 'un thread par connexion' explose en coût de RESSOURCES : chaque thread OS réserve une pile (souvent ~1 Mo, plusieurs Go pour 50k threads), un descripteur de tâche kernel, et surtout le scheduler passe son temps en context-switches (chacun ~1-5 µs + pollution de cache) pour des threads qui ne font qu'attendre. L'event-loop (epoll/kqueue) MULTIPLEXE des milliers de sockets sur un petit nombre de threads : un seul appel bloquant (`epoll_wait`) réveille exactement les sockets prêts, sans bloquer un thread par connexion inactive. Distracteurs : il n'y a pas de 'GIL du kernel' — les threads OS s'exécutent bien sur plusieurs cœurs (et l'async est souvent mono-thread par event-loop) ; les threads OS PEUVENT utiliser epoll/I/O non bloquantes (le modèle bloquant est un choix, pas une fatalité) ; l'async ne supprime pas la latence réseau physique. Le gain est la scalabilité mémoire/scheduling face aux connexions en attente.","tags":["io-bound","async","epoll","threads"],"optionNotes":["50k threads = Go de piles + context-switches incessants ; l'event-loop multiplexe sur peu de threads.","Faux : il n'existe pas de « GIL du kernel » ; les threads OS tournent sur plusieurs cœurs.","Faux : les threads OS peuvent utiliser epoll/I/O non bloquantes ; le blocage est un choix.","Faux : l'async ne supprime pas la latence réseau physique."]},
  {"id":"perf-insertion-en-tete-repetee-dans-un-arrayl","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Insertion en tête répétée dans un ArrayList","company":"Java docs — ArrayList (« add at index shifts elements ») ; CLRS ch. 10 (choix de structure)","language":"java","code":"List<Event> ordered = new ArrayList<>();\nfor (Event e : incoming) {         // 500_000 events\n    ordered.add(0, e);             // on veut le plus recent en tete\n}","question":"Quelle est la complexité et quelle structure choisir à la place ?","options":["O(n²) : `add(0, e)` sur un ArrayList décale TOUS les éléments existants d'un cran (System.arraycopy) à chaque insertion ; utiliser un ArrayDeque (addFirst en O(1) amorti) ou ajouter en fin puis inverser une fois","O(n log n) : chaque insertion en position 0 force un rééquilibrage de l'arbre interne de l'ArrayList ; passer à une LinkedList élimine le rééquilibrage","O(n) déjà optimal : `add(0, e)` est O(1) car ArrayList maintient un pointeur de tête circulaire ; le vrai coût est le redimensionnement du tableau","O(n²) mais irréductible : tout conteneur ordonné impose un décalage lors d'une insertion en tête ; il faut accepter le coût ou dénormaliser en base"],"answer":0,"explanation":"`ArrayList.add(0, e)` insère en position 0, ce qui oblige à DÉCALER tous les éléments déjà présents d'un indice vers la droite (via `System.arraycopy`), soit O(taille_actuelle) par insertion. Répété sur n=500k événements, cela donne 1+2+…+n = O(n²) ≈ 1,25×10^11 déplacements — plusieurs minutes. Solutions : `ArrayDeque.addFirst(e)` est O(1) amorti (buffer circulaire), ou `LinkedList.addFirst` O(1) (mais mauvais cache), ou plus simplement `add(e)` en fin (O(1) amorti) puis `Collections.reverse` une seule fois O(n). Distracteurs : ArrayList n'a PAS d'arbre interne ni de pointeur de tête circulaire (c'est un tableau à base fixe en 0) ; le coût n'est pas le redimensionnement mais le décalage ; ce n'est pas irréductible (ArrayDeque le résout). Cas typique du mauvais choix de structure pour le motif d'accès.","tags":["complexite","arraylist","deque","java"],"optionNotes":["add(0, e) décale tous les éléments (arraycopy) à chaque insertion → O(n²) ; ArrayDeque est O(1).","Faux : ArrayList n'a pas d'arbre interne à rééquilibrer.","Faux : ArrayList est un tableau à base 0, sans pointeur de tête circulaire.","Faux : ce n'est pas irréductible ; ArrayDeque ou add+reverse le résout."]},
  {"id":"perf-work-stealing-pourquoi-voler-en-tete-de-","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Work stealing : pourquoi voler en tête de deque","company":"Blumofe & Leiserson, « Scheduling Multithreaded Computations by Work Stealing » (1999) ; Java ForkJoinPool doc","question":"Dans un ordonnanceur work-stealing (Cilk, ForkJoinPool, Tokio), chaque worker possède une double-ended queue (deque) de tâches. Pourquoi le worker propriétaire pousse/prend-il par le BAS pendant que les voleurs prennent par le HAUT ?","options":["Le propriétaire opère en LIFO sur son bas (meilleure localité de cache, tâches récentes encore chaudes) sans synchronisation contended ; les voleurs prennent en FIFO par le haut les tâches les plus ANCIENNES/grosses, minimisant la contention et maximisant le grain volé","Le voleur doit prendre par le haut car les tâches y sont déjà terminées et libèrent le propriétaire ; prendre par le bas volerait des tâches en cours d'exécution, causant des corruptions","Le propriétaire prend par le bas pour respecter l'ordre de soumission FIFO exigé par la sémantique fork/join, tandis que les voleurs, non liés à cet ordre, prennent arbitrairement par le haut","Cette séparation permet d'utiliser deux verrous distincts (un par extrémité), doublant le débit d'accès à la deque par rapport à une pile classique à verrou unique"],"answer":0,"explanation":"La géométrie de la deque est un choix de PERFORMANCE. Le worker propriétaire travaille sur SON extrémité (le bas) en mode LIFO : les tâches qu'il vient de créer (fork récent) sont les plus susceptibles d'avoir des données encore chaudes en cache → excellente localité, et ces opérations pop/push locales n'entrent presque jamais en collision avec les voleurs (pas de synchronisation lourde dans le cas commun). Les VOLEURS, eux, prennent à l'AUTRE bout (le haut) les tâches les plus ANCIENNES : dans un calcul divide-and-conquer, ce sont les plus GROSSES (haut de l'arbre), donc voler une seule tâche rapporte beaucoup de travail → moins de vols nécessaires, contention minimale, et les deux extrémités se disputent rarement le même élément. Distracteurs : les tâches en haut ne sont pas 'terminées' ; ce n'est pas une contrainte FIFO de fork/join ; ce n'est pas une histoire de 'deux verrous' — les deques work-stealing modernes sont largement lock-free (Chase-Lev), le point est la localité + faible contention.","tags":["work-stealing","deque","concurrence","scheduler"],"optionNotes":["Propriétaire en LIFO local (cache chaud, sans contention) ; voleurs prennent les grosses tâches anciennes en haut.","Faux : les tâches du haut ne sont pas « terminées ».","Faux : ce n'est pas une contrainte FIFO du fork/join.","Faux : les deques modernes (Chase-Lev) sont lock-free ; l'enjeu est localité + faible contention."]},
  {"id":"perf-requete-sans-batch-commit-par-ligne","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Requête sans batch : commit par ligne","company":"Brendan Gregg, Systems Performance 2e éd. (I/O, fsync) ; doc transactions SQL (autocommit) ; batching/amortissement","language":"python","code":"def import_rows(rows, conn):\n    cur = conn.cursor()\n    for r in rows:                 # 1_000_000 lignes\n        cur.execute(\n            \"INSERT INTO metrics(ts, val) VALUES (%s, %s)\", (r.ts, r.val))\n        conn.commit()\n    return len(rows)","question":"L'import de 1M lignes prend des heures alors que la table est petite. Cause principale et remède ?","options":["Un `commit()` par ligne force un fsync disque synchrone à chaque insertion (durabilité WAL) : ~1M fsync × quelques ms chacun. Batcher en committant tous les N (ex. 1000-10000) lignes, ou une seule transaction, amortit le coût de synchronisation de plusieurs ordres de grandeur","`cur.execute` recompile le plan de la requête à chaque appel ; utiliser une requête préparée une seule fois divise le temps par le nombre de lignes, le commit n'ayant qu'un impact marginal","Le curseur accumule 1M résultats en mémoire côté client faute de `fetch`, provoquant un swap ; consommer/fermer le curseur régulièrement résout la lenteur","L'`INSERT` ligne par ligne verrouille toute la table à chaque appel, sérialisant d'éventuels lecteurs ; passer en INSERT ... ON CONFLICT lève ces verrous et accélère"],"answer":0,"explanation":"Le tueur est le `commit()` PAR LIGNE. Chaque commit garantit la durabilité en forçant un FSYNC du write-ahead log sur le disque — une opération synchrone coûteuse (de ~0,1 ms sur SSD à plusieurs ms sur disque, limitée par la latence physique du stockage, pas par le CPU). 1 million de fsync sérialisés = des heures, indépendamment de la taille de la table. Le remède est le BATCHING/amortissement : committer tous les 1000-10000 inserts (voire une seule transaction globale) fait passer le nombre de fsync de 10^6 à ~10^3-10^0, amortissant le coût fixe de synchronisation sur des milliers de lignes. Encore mieux : `executemany`/COPY. Distracteurs : la recompilation de plan est réelle mais secondaire face aux fsync (préparer aide, mais sans batcher le commit reste dominant) ; ce n'est pas un problème de fetch (INSERT ne renvoie pas de lignes) ; le verrouillage de table n'est pas le facteur (les inserts n'ont pas de lecteurs concurrents ici). Amortir le coût fixe est le principe.","tags":["batching","commit","fsync","sql"],"optionNotes":["Un commit par ligne force ~1M fsync disque synchrones ; batcher tous les N amortit la synchro.","Faux : la recompilation de plan est réelle mais secondaire face aux fsync.","Faux : un INSERT ne renvoie pas de lignes ; pas d'accumulation côté client.","Faux : les inserts n'ont pas de lecteurs concurrents ici ; les verrous ne dominent pas."]},
  {"id":"perf-cache-lru-invalidation-par-ttl-vs-par-ev","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Cache LRU : invalidation par TTL vs par événement","company":"Brendan Gregg, Systems Performance ch. 2 (caching) ; Phil Karlton « two hard things » ; patterns cache-aside","question":"Un cache applicatif accélère énormément les lectures, mais votre équipe débat de la stratégie d'invalidation. Quelle affirmation est correcte sur le compromis TTL vs invalidation explicite ?","options":["Le TTL est simple et borne la fraîcheur à l'intervalle choisi, au prix de servir des données périmées jusqu'à expiration ; l'invalidation explicite (sur write) donne une cohérence plus forte mais exige de tracer toutes les dépendances, source de bugs de clés oubliées","Le TTL garantit une cohérence forte car les données expirent avant toute modification, alors que l'invalidation explicite est intrinsèquement incohérente sous concurrence ; le TTL est donc toujours préférable","L'invalidation explicite est sans coût car elle ne s'exécute que sur écriture, et élimine tout risque de donnée périmée en toutes circonstances, y compris sous partition réseau ou écritures concurrentes","Le TTL et l'invalidation explicite sont équivalents en cohérence ; le seul critère de choix est la consommation mémoire, le TTL libérant les entrées plus tôt"],"answer":0,"explanation":"Comme le dit l'adage (Karlton) : les deux choses difficiles en informatique sont l'invalidation de cache et le nommage. Le compromis réel : le TTL (expiration temporelle) est TRIVIAL à implémenter et BORNE la péremption à l'intervalle — mais on sert potentiellement des données périmées pendant toute la durée du TTL après un changement. L'invalidation EXPLICITE (purge/mise à jour de la clé lors d'une écriture) offre une cohérence plus FORTE, mais exige de connaître et suivre TOUTES les dépendances de chaque entrée : c'est là que naissent les bugs (clé oubliée, invalidation partielle, races entre read-miss-repopulate et write-invalidate). Distracteurs : le TTL ne garantit PAS la cohérence forte (données stale jusqu'à expiration) — inverse du distracteur 2 ; l'invalidation explicite N'EST PAS sans coût ni infaillible (les races et partitions cassent la cohérence, cf. patterns cache-aside) ; TTL et invalidation ne sont pas équivalents en cohérence. En pratique on combine souvent TTL (filet de sécurité) + invalidation ciblée.","tags":["caching","invalidation","ttl","coherence"],"optionNotes":["TTL simple mais sert du périmé jusqu'à expiration ; l'explicite est plus cohérent mais exige de tracer les dépendances.","Faux : le TTL ne garantit pas la cohérence forte (données stale jusqu'à expiration).","Faux : l'invalidation explicite a un coût et casse sous races/partitions.","Faux : TTL et invalidation explicite ne sont pas équivalents en cohérence."]},
  {"id":"perf-micro-benchmark-dead-code-elimination","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Micro-benchmark : dead code elimination","company":"JMH doc (Blackhole, DCE) ; A. Shipilëv « Nanotrusting the Nanotime » ; Brendan Gregg, benchmarking pitfalls","language":"java","code":"long start = System.nanoTime();\nfor (int i = 0; i < 100_000_000; i++) {\n    Math.sqrt(i);          // on veut mesurer sqrt\n}\nlong ns = System.nanoTime() - start;\nSystem.out.println(\"sqrt: \" + ns / 100_000_000.0 + \" ns/op\");","question":"Ce benchmark rapporte ~0 ns/op, un résultat absurde. Pourquoi, et comment le corriger ?","options":["Le résultat de `Math.sqrt(i)` n'est jamais utilisé : le JIT applique le dead-code elimination et supprime tout l'appel. Il faut consommer le résultat (l'accumuler dans une variable lue ensuite, ou un Blackhole JMH) pour empêcher l'élimination","`System.nanoTime()` sature à sa résolution minimale sur 10^8 itérations, tronquant la différence à zéro ; mesurer en nanosecondes discrètes par lot de 1000 rétablit une valeur correcte","La boucle est entièrement déroulée par le compilateur en une seule instruction SIMD sqrt vectorielle, donc le temps mesuré est réellement quasi nul et le résultat est correct","`ns / 100_000_000.0` divise par un `double` ce qui arrondit le quotient à 0 par perte de précision flottante ; caster en `long` avant la division corrige l'affichage"],"answer":0,"explanation":"Le piège emblématique du micro-benchmarking : le DEAD CODE ELIMINATION. Comme la valeur de retour de `Math.sqrt(i)` n'est jamais lue ni utilisée, le compilateur JIT prouve que l'appel n'a aucun effet observable et l'ÉLIMINE entièrement — la boucle devient vide (voire supprimée aussi), d'où ~0 ns/op. Correction : rendre le résultat OBSERVABLE, par ex. accumuler `sink += Math.sqrt(i)` et imprimer/consommer `sink` après le chrono, ou utiliser `Blackhole.consume()` de JMH prévu exactement pour ça. Distracteurs : `nanoTime()` ne sature pas à zéro sur 10^8 itérations (l'intervalle total serait des dizaines de ms, parfaitement mesurables — le vrai zéro vient du DCE, pas de la résolution) ; le compilateur ne remplace pas la boucle par une seule instruction magique ; la division par 100_000_000.0 en double ne donne pas 0 (elle est correcte, le numérateur est ~0). Un benchmark valide doit défaire le DCE, faire du warmup et gérer la variance.","tags":["benchmarking","dead-code-elimination","jit","jmh"],"optionNotes":["Le résultat de sqrt n'est jamais lu : le JIT applique le dead-code elimination et supprime l'appel.","Faux : sur 10^8 itérations l'intervalle est mesurable ; nanoTime ne sature pas à zéro.","Faux : le compilateur ne remplace pas la boucle par une instruction magique.","Faux : la division par un double est correcte, le numérateur est déjà ~0."]},
  {"id":"perf-gc-generational-hypothese-generationnell","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"GC generational : hypothèse générationnelle","company":"Brendan Gregg, Systems Performance ch. 5 ; Jones & Hosking, The Garbage Collection Handbook (weak generational hypothesis)","question":"Les GC générationnels (JVM, .NET, V8) séparent les objets en jeune/vieille génération. Sur quelle hypothèse cette conception repose-t-elle, et quel motif d'allocation la met en défaut ?","options":["Hypothèse générationnelle faible : « la plupart des objets meurent jeunes ». On collecte souvent la jeune génération (rapide, peu de survivants à copier). Un motif à durée de vie MOYENNE (objets survivant juste assez pour être promus puis mourir) casse l'hypothèse et gonfle la vieille génération","Hypothèse que les objets anciens ne référencent jamais les jeunes ; elle est mise en défaut par les structures de données mutables où un vieil objet pointe vers un nouveau, cas impossible à gérer sans full GC","Hypothèse que la jeune génération tient toujours en cache L2 ; un pic d'allocation dépassant la taille du cache force un fallback en mémoire principale qui annule tout gain du GC générationnel","Hypothèse que le débit d'allocation est constant ; toute rafale d'allocation en burst déclenche une promotion immédiate en vieille génération, contournable en fixant une taille d'Eden énorme"],"answer":0,"explanation":"Le fondement est l'HYPOTHÈSE GÉNÉRATIONNELLE FAIBLE : empiriquement, la vaste majorité des objets meurent très jeunes (temporaires, résultats intermédiaires). Le GC exploite cela en collectant fréquemment la seule JEUNE génération (Eden/survivor) : c'est rapide car peu d'objets survivent, et un collecteur par copie ne travaille que sur les survivants (proportionnel aux vivants, pas aux morts). Les objets qui survivent à plusieurs collectes mineures sont PROMUS en vieille génération, collectée rarement. Le motif qui DÉGRADE ce schéma : les objets à durée de vie MOYENNE — assez longue pour être promus mais qui meurent peu après — polluent la vieille génération, forçant des GC majeurs coûteux (souvent avec pauses stop-the-world plus longues). Distracteurs : les vieux→jeunes references EXISTENT et sont gérées efficacement par les write barriers/card tables (pas besoin de full GC) ; le GC générationnel ne suppose pas que la jeune gen tienne en L2 ; il ne suppose pas un débit d'allocation constant. Réduire la pression GC = minimiser les objets à durée de vie moyenne et le taux d'allocation.","tags":["gc","generational","memoire","jvm"],"optionNotes":["Hypothèse faible : la plupart des objets meurent jeunes ; les objets de vie moyenne polluent la vieille gen.","Faux : les références vieux→jeunes sont gérées par write barriers/card tables, sans full GC.","Faux : le GC ne suppose pas que la jeune gen tienne en L2.","Faux : le GC ne suppose pas un débit d'allocation constant."]},
  {"id":"perf-stringbuilder-mal-dimensionne-vs-concat-","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"StringBuilder mal dimensionné vs concat — piège inverse","company":"Java docs — StringBuilder capacity ; JLS 15.18.1 (string concat compilée en StringBuilder) ; Effective Java Item 63","language":"java","code":"String join(List<String> parts) {\n    StringBuilder sb = new StringBuilder();\n    for (int i = 0; i < parts.size(); i++) {\n        sb.append(parts.get(i));\n        if (i < parts.size() - 1) {\n            sb = new StringBuilder(sb.toString() + \",\");\n        }\n    }\n    return sb.toString();\n}","question":"Ce code utilise un StringBuilder mais reste O(n²). Où est l'erreur exacte ?","options":["La ligne `sb = new StringBuilder(sb.toString() + \",\")` recrée un StringBuilder à partir de la chaîne entière accumulée à chaque itération : c'est une COPIE O(taille) par tour, annulant l'intérêt du StringBuilder et redonnant O(n²)","`parts.get(i)` sur une List est O(n) si la liste est une LinkedList, ce qui rend la boucle O(n²) indépendamment du StringBuilder","`sb.append` sans capacité initiale redimensionne le buffer à chaque append par pas de 1 caractère, provoquant n réallocations O(n)","`parts.size()` réévalué dans la condition force un recomptage O(n) de la liste à chaque itération, cause principale du O(n²)"],"answer":0,"explanation":"L'erreur est la ligne `sb = new StringBuilder(sb.toString() + \",\")`. À chaque itération, on : (1) `sb.toString()` — copie tout le contenu accumulé en une String O(taille) ; (2) concatène `+ \",\"` — encore une copie ; (3) `new StringBuilder(...)` — recopie tout dans un nouveau buffer. On paie donc O(taille_courante) par itération, et la somme redonne O(n²) — exactement le fléau que StringBuilder est censé éviter. La correction : NE PAS reconstruire le builder ; faire simplement `sb.append(parts.get(i)); if (...) sb.append(\",\");`, ce qui donne O(n) amorti (le buffer interne croît géométriquement). Distracteurs : si la List est une ArrayList, `get(i)` est O(1) (et l'énoncé n'indique pas LinkedList) ; `append` sans capacité initiale utilise une croissance GÉOMÉTRIQUE du buffer (amorti O(1)), pas par pas de 1 — donc pas O(n²) ; `size()` sur une List usuelle est O(1). Le vrai coupable est la reconstruction complète du builder.","tags":["stringbuilder","complexite","java","quadratique"],"optionNotes":["Reconstruire le builder via sb.toString()+\",\" recopie tout l'accumulé à chaque tour → O(n²).","Faux : sur une ArrayList get(i) est O(1) ; l'énoncé n'indique pas LinkedList.","Faux : append croît géométriquement (amorti O(1)), pas par pas de 1 caractère.","Faux : size() sur une List usuelle est O(1)."]},
  {"id":"perf-hash-map-o-1-amorti-et-son-pire-cas","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Hash map : O(1) amorti et son pire cas","company":"CLRS ch. 11 (hashing) ; Java 8 HashMap treeification (JEP) ; CWE-407 (algorithmic complexity DoS)","question":"Une hash map offre des opérations O(1) AMORTI. Dans quelles conditions dégénère-t-elle en O(n) par opération, et comment Java 8 a-t-il atténué cela ?","options":["Quand trop de clés tombent dans le même bucket (mauvaise fonction de hash ou collisions adverses), le bucket devient une liste O(n) ; Java 8 convertit un bucket trop long (≥8 avec table ≥64) en arbre rouge-noir, ramenant le pire cas du bucket à O(log n)","Quand le facteur de charge dépasse 1, la map arrête de redimensionner pour économiser la mémoire et toutes les opérations deviennent O(n) ; Java 8 a supprimé cette limite en autorisant un facteur de charge illimité","Quand deux threads insèrent simultanément, la map entre en O(n) le temps de résoudre le conflit de verrou ; Java 8 a rendu HashMap thread-safe par défaut, éliminant cette dégradation","Quand les clés sont des String longues, le calcul du hashCode devient O(longueur) donc chaque opération est O(n) ; Java 8 met en cache le hashCode dans l'objet String pour amortir ce coût"],"answer":0,"explanation":"Le O(1) d'une hash map est AMORTI et suppose une bonne distribution. Le pire cas O(n) survient quand de nombreuses clés COLLISIONNENT dans le même bucket : soit par une fonction de hachage médiocre, soit par une attaque adverse (hash flooding, CWE-407) où l'attaquant forge des clés de même hash pour provoquer un déni de service. Le bucket devient alors une liste chaînée parcourue en O(n). Java 8 a ajouté la TREEIFICATION : quand un bucket dépasse 8 éléments (et que la table fait ≥64 entrées), il est converti en arbre rouge-noir, bornant les opérations de ce bucket à O(log n) au lieu de O(n). Distracteurs : le facteur de charge >1 déclenche un RESIZE (pas un arrêt du redimensionnement), Java n'a pas 'supprimé la limite' ; HashMap n'est PAS thread-safe en Java 8 (c'est ConcurrentHashMap qui l'est) ; le hashCode de String est bien caché mais ce n'est pas le mécanisme d'atténuation des collisions, et ce n'est pas ce qui cause la dégénérescence O(n) principale (les collisions le sont).","tags":["hashmap","complexite","collisions","treeification"],"optionNotes":["Trop de collisions dans un bucket le rendent O(n) ; Java 8 le convertit en arbre rouge-noir (O(log n)).","Faux : un load factor >1 déclenche un resize, pas un arrêt du redimensionnement.","Faux : HashMap n'est pas thread-safe en Java 8 (c'est ConcurrentHashMap).","Faux : le hashCode de String est caché ; ce n'est pas la cause de la dégénérescence O(n)."]},
  {"id":"perf-lecture-fichier-octet-par-octet","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Lecture fichier octet par octet","company":"Brendan Gregg, Systems Performance ch. 8 (filesystem, buffered I/O) ; Bentley, Programming Pearls (I/O batching)","language":"python","code":"def sha_of(path):\n    import hashlib\n    h = hashlib.sha256()\n    with open(path, \"rb\") as f:\n        byte = f.read(1)\n        while byte:\n            h.update(byte)\n            byte = f.read(1)\n    return h.hexdigest()","question":"Sur un fichier de 2 Go, cette fonction est extrêmement lente. Cause dominante et correction ?","options":["`f.read(1)` fait 2 milliards d'appels avec un overhead par appel (traversée de la couche I/O bufferisée, boucle Python) : lire par gros blocs `f.read(1 << 20)` (1 Mo) amortit ce coût par un facteur ~10^6 en nombre d'itérations et d'appels","`hashlib.sha256().update` réinitialise l'état de hachage à chaque appel sur 1 octet, forçant un recalcul complet du digest partiel, ce qui rend l'algorithme O(n²)","`open(path, \"rb\")` sans argument buffering désactive le cache disque du système, forçant un syscall read() par octet directement vers le contrôleur SSD, saturant la file d'I/O","La boucle `while byte` reconstruit un objet bytes de taille croissante à chaque tour, provoquant une accumulation mémoire de 2 Go et un swap continu"],"answer":0,"explanation":"Lire OCTET PAR OCTET est l'anti-pattern. `f.read(1)` sur 2 Go = 2 milliards d'itérations Python, chacune avec un appel de méthode, un aller-retour dans la couche d'I/O bufferisée, la création d'un objet bytes d'1 octet, et un `h.update` sur 1 octet. Même si le buffer applicatif de Python évite un vrai syscall par octet, l'OVERHEAD PAR APPEL/ITÉRATION domine totalement. La correction est le BATCHING : lire par gros blocs, `while chunk := f.read(1 << 20)` (1 Mo), ce qui divise le nombre d'itérations et d'appels par ~10^6, saturant le débit séquentiel du disque et amortissant le coût fixe. Distracteurs : `update` NE réinitialise PAS l'état (SHA-256 est incrémental, update sur des morceaux successifs est correct et O(total), pas O(n²)) ; `open(..., 'rb')` ne désactive pas le buffering (buffering par défaut actif) — mais même bufferisé, l'overhead par appel Python reste le fléau ; le `read(1)` ne fait pas grossir un buffer de 2 Go (chaque byte fait 1 octet). Le principe : amortir le coût fixe par de gros blocs.","tags":["io","batching","buffered-io","python"],"optionNotes":["read(1) fait 2 milliards d'itérations à fort overhead ; lire par blocs de 1 Mo l'amortit.","Faux : SHA-256 est incrémental ; update sur des morceaux est O(total), pas O(n²).","Faux : open('rb') bufferise par défaut ; le fléau est l'overhead par appel Python.","Faux : chaque read(1) fait 1 octet ; aucun buffer de 2 Go ne grossit."]},
  {"id":"perf-selection-de-mediane-ne-pas-trier-pour-t","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Sélection de médiane : ne pas trier pour trouver le k-ième","company":"CLRS ch. 9 (Medians and Order Statistics, Quickselect) ; Bentley, Programming Pearls","question":"Pour trouver la médiane d'un tableau de n éléments non triés, un développeur écrit `sort(a); return a[n/2]` en O(n log n). Quelle alternative est asymptotiquement meilleure et quel est son compromis ?","options":["Quickselect (partition à la Hoare) trouve le k-ième élément en O(n) en MOYENNE, sans trier tout le tableau, mais son pire cas est O(n²) sur de mauvais pivots ; l'algorithme median-of-medians garantit O(n) au pire au prix d'une constante plus élevée","Un tas binaire (heap) de taille n/2 trouve la médiane en O(n) au pire cas garanti, ce qui domine strictement Quickselect en toutes circonstances et sans aucun compromis","Trier avec un radix sort ramène le tri à O(n) donc `sort(a); a[n/2]` devient optimal ; Quickselect n'apporte rien de plus et est plus difficile à implémenter correctement","Une recherche dichotomique sur les valeurs trouve la médiane en O(log n) en comptant les éléments inférieurs, bien plus rapide que tout parcours linéaire du tableau"],"answer":0,"explanation":"Trier pour ne récupérer QU'UN élément gaspille du travail : le tri produit l'ordre total (O(n log n)) alors qu'on ne veut que le k-ième. QUICKSELECT (Hoare) partitionne autour d'un pivot et ne récurse que du côté contenant le rang cherché : espérance O(n) (récurrence T(n)=T(n/2)+O(n) ≈ 2n), sans jamais trier entièrement. Compromis : pivot mal choisi → pire cas O(n²) (comme quicksort). L'algorithme MEDIAN-OF-MEDIANS (BFPRT) garantit O(n) au PIRE cas via un pivot de bonne qualité, mais avec une constante bien plus grande (souvent plus lent en pratique que quickselect randomisé). Distracteurs : un heap trouve le k-ième en O(n + k log n) (pour k=n/2 c'est O(n log n), pas O(n) garanti) — faux ; le radix sort est O(n) seulement sous hypothèses fortes (clés bornées/entières) et n'est pas général ni 'optimal' ici ; on ne peut PAS faire une dichotomie sur des valeurs non triées sans coût de comptage O(n) par étape (l'option 4 ignore ce coût). Le principe : ne calcule que ce dont tu as besoin.","tags":["quickselect","complexite","median","algorithme"],"optionNotes":["Quickselect trouve le k-ième en O(n) moyen sans trier ; median-of-medians garantit O(n) au pire.","Faux : un heap trouve le k=n/2 en O(n log n), pas O(n) garanti.","Faux : radix sort n'est O(n) que sous hypothèses fortes ; pas général ni « optimal » ici.","Faux : impossible de faire une dichotomie sur des valeurs non triées sans comptage O(n) par étape."]},
  {"id":"perf-contention-sur-un-compteur-atomique-glob","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Contention sur un compteur atomique global","company":"Brendan Gregg, Systems Performance ch. 6 (atomics, cache coherence) ; Java LongAdder doc (striped counters)","language":"java","code":"class Metrics {\n    private final AtomicLong requests = new AtomicLong();\n    // appele par 64 threads en parallele, tres frequemment\n    void onRequest() {\n        requests.incrementAndGet();\n    }\n    long total() { return requests.get(); }\n}","question":"Sous 64 threads très actifs, ce compteur atomique devient un goulot. Pourquoi, et quelle est la bonne structure ?","options":["`incrementAndGet` fait un CAS en boucle sur UNE seule ligne de cache disputée par 64 cœurs : sous forte contention, la plupart des CAS échouent et retentent (cache-line ping-pong), effondrant le débit. `LongAdder` répartit le compte sur plusieurs cellules par thread, sommées à la lecture","`AtomicLong` prend un verrou mutex interne à chaque incrément, sérialisant les 64 threads ; remplacer par un `long volatile` avec `++` supprime le verrou et corrige la contention","`incrementAndGet` alloue un objet Long à chaque appel (boxing), saturant le GC sous 64 threads ; utiliser un `AtomicInteger` évite l'allocation et résout le problème","Le problème est `total()` qui bloque tous les incréments le temps de la lecture cohérente ; espacer les appels à total() rétablit le débit d'incrémentation"],"answer":0,"explanation":"`AtomicLong.incrementAndGet` implémente un CAS (compare-and-swap) en boucle sur une UNIQUE variable, donc une seule ligne de cache. Sous 64 threads très actifs, cette ligne fait un ping-pong permanent entre les caches des cœurs (cohérence), et beaucoup de CAS échouent (un autre thread a modifié la valeur entre le read et le CAS) → retry, gaspillage massif, débit qui s'effondre malgré l'absence de verrou. La solution conçue pour ça est `LongAdder` : il maintient plusieurs CELLULES (une base + un tableau de cellules dispersées, façon striping) que différents threads incrémentent séparément, éliminant la contention ; `sum()` additionne les cellules à la lecture. Distracteurs : `AtomicLong` n'utilise PAS de mutex (c'est du lock-free CAS), et `long volatile` avec `++` n'est PAS atomique (perte d'incréments) ; `incrementAndGet` ne fait pas de boxing (opère sur un long primitif interne) ; `total()`/`get()` ne bloque pas les incréments (lecture non bloquante). Le vrai fléau est la contention sur la ligne de cache unique.","tags":["contention","atomic","longadder","concurrence"],"optionNotes":["incrementAndGet fait un CAS en boucle sur une seule ligne de cache disputée ; LongAdder répartit sur des cellules.","Faux : AtomicLong est lock-free (pas de mutex) ; long volatile ++ n'est pas atomique.","Faux : incrementAndGet opère sur un long primitif, sans boxing.","Faux : get()/total() est une lecture non bloquante."]},
  {"id":"perf-latency-numbers-compression-vs-reseau","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Latency numbers : compression vs réseau","company":"Jeff Dean, « Latency Numbers Every Programmer Should Know » ; Brendan Gregg, Systems Performance ch. 2","question":"Vous envoyez 1 Mo sur un lien 1 Gbps. Compresser d'abord avec un codec rapide type Snappy/Zippy (~3 µs pour 1 Ko, débit ~250-500 Mo/s) est-il rentable ? Raisonnement chiffré attendu.","options":["Envoyer 1 Mo à 1 Gbps ≈ 10 ms (car 1 Ko ≈ 10 µs) ; compresser 1 Mo à ~300 Mo/s ≈ 3 ms. Si le ratio réduit le volume réseau de moitié, on économise ~5 ms sur les 10 ms au prix de ~3 ms de CPU : rentable quand le réseau est le goulot et les données compressibles","Envoyer 1 Mo à 1 Gbps ≈ 1 ms ; compresser prend ~3 ms, donc la compression coûte toujours plus cher que l'envoi et n'est jamais rentable sur un lien 1 Gbps, quel que soit le ratio","Envoyer 1 Mo à 1 Gbps ≈ 100 ms ; compresser en ~3 ms est négligeable donc la compression est toujours rentable, indépendamment du taux de compressibilité des données","Envoyer 1 Mo à 1 Gbps ≈ 10 µs ; compresser prend ~3 ms soit 300× plus, donc la compression est absurde ici et dégrade systématiquement la latence"],"answer":0,"explanation":"Repère de Jeff Dean : envoyer 1 Ko sur 1 Gbps ≈ 10 µs, donc 1 Mo (1000 Ko) ≈ 10 ms (le calcul brut : 1 Mo = 8 Mbit, à 1 Gbps = 8 ms, cohérent avec ~10 ms). Compresser 1 Mo à ~300 Mo/s prend ~3 ms de CPU. Si les données se compriment d'un facteur ~2, le transfert tombe à ~5 ms : bilan ≈ 3 ms CPU + 5 ms réseau = 8 ms contre 10 ms, gain net QUAND le réseau est le goulot et les données compressibles (texte, JSON). Sur données déjà compressées (images, chiffré) le ratio ≈ 1 : on paie le CPU sans rien gagner → non rentable. Distracteurs : 1 Mo à 1 Gbps n'est ni 1 ms (option 2, sous-estime ×10) ni 100 ms (option 3, surestime ×10) ni 10 µs (option 4, confond 1 Ko et 1 Mo). Le point : raisonner avec les ordres de grandeur ET la compressibilité réelle, pas appliquer la compression aveuglément.","tags":["latency-numbers","compression","reseau","tradeoff"],"optionNotes":["1 Mo à 1 Gbps ≈ 10 ms ; compresser (~3 ms) et diviser le volume par 2 fait gagner du temps net.","Faux : 1 Mo à 1 Gbps ≈ 10 ms, pas 1 ms (sous-estime ×10).","Faux : ≈ 10 ms, pas 100 ms ; et sur données incompressibles c'est perdant.","Faux : 10 µs confond 1 Ko et 1 Mo (facteur 1000)."]},
  {"id":"perf-recalcul-repete-dans-une-jointure-manuel","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Recalcul répété dans une jointure manuelle","company":"Bentley, Programming Pearls ch. 8 (precompute, hashing) ; CLRS ch. 11 (join via hash)","language":"python","code":"def match_transactions(payments, invoices):\n    matched = []\n    for p in payments:                 # 50_000\n        for inv in invoices:           # 50_000\n            if p.ref == inv.ref and p.amount == inv.amount:\n                matched.append((p, inv))\n    return matched","question":"Cette jointure manuelle est en O(n·m). Quelle refonte la ramène à O(n+m) et sur quel principe ?","options":["Construire un index de hachage des invoices sur la clé composite `(ref, amount)` une fois — O(m) — puis pour chaque paiement chercher directement dans ce dict — O(1) amorti — soit O(n+m) : c'est une hash join qui remplace le produit cartésien","Trier les deux listes par `ref` puis fusionner en O(n log n) est la seule option sous-quadratique possible ; aucune structure ne permet d'atteindre O(n+m) sur une jointure à deux critères","Remplacer les boucles imbriquées par une compréhension de liste réduit intrinsèquement la complexité à O(n) car les compréhensions Python sont exécutées en C sans double parcours","Paralléliser la boucle externe sur k cœurs divise le temps par k, ramenant la complexité à O(n·m / k) qui est la meilleure atteignable pour une correspondance à deux champs"],"answer":0,"explanation":"Les boucles imbriquées réalisent un PRODUIT CARTÉSIEN O(n·m) = 50 000 × 50 000 = 2,5 milliards de comparaisons. La refonte canonique est une HASH JOIN : construire un dictionnaire indexant les invoices par leur clé composite `(inv.ref, inv.amount)` en O(m) (une passe), puis pour chaque paiement, faire un lookup direct `index.get((p.ref, p.amount))` en O(1) amorti, total O(n+m) — un gain de ~5 ordres de grandeur ici. Principe de Bentley : précalculer/indexer pour transformer une recherche répétée en accès direct. Distracteurs : le tri-fusion (O(n log n)) est sous-quadratique mais N'EST PAS la seule option (la hash join fait mieux, O(n+m)) — l'option 2 est fausse ; une compréhension de liste ne change PAS la complexité (elle reste O(n·m) si elle contient la même double itération) ; paralléliser ne change pas la COMPLEXITÉ asymptotique O(n·m) (juste la constante), et reste bien pire qu'un changement d'algorithme.","tags":["hash-join","complexite","index","quadratique"],"optionNotes":["Indexer les invoices par (ref, amount) puis lookup O(1) par paiement : hash join O(n+m).","Faux : le tri-fusion (O(n log n)) n'est pas la seule option ; la hash join fait mieux.","Faux : une compréhension avec la même double itération reste O(n·m).","Faux : paralléliser ne change pas la complexité O(n·m), juste la constante."]},
  {"id":"perf-micro-vs-macro-benchmark-le-piege-de-la-","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Micro vs macro benchmark : le piège de la représentativité","company":"Brendan Gregg, Systems Performance ch. 12 (benchmarking, active benchmarking) ; « benchmarking sins »","question":"Un micro-benchmark montre que la fonction B est 3× plus rapide que A. Après déploiement, le débit global ne bouge pas. Quelle explication est la plus alignée avec une démarche de benchmarking rigoureuse ?","options":["La fonction optimisée n'était pas sur le hot path réel ou ne représentait qu'une part infime du temps total (Amdahl) ; un micro-benchmark isolé ignore le contexte (cache froid en prod, contention, I/O) que seul un macro-benchmark représentatif capture","Le micro-benchmark était forcément faux car il n'a pas mesuré B en production ; tout micro-benchmark est intrinsèquement non fiable et doit être proscrit au profit des seuls tests en production réelle","Le compilateur a dévectorisé B lors du déploiement car les flags de production diffèrent, annulant le gain ; réaligner les flags de compilation restaure toujours le 3× mesuré","Le débit global est plafonné par le garbage collector qui masque tout gain applicatif ; désactiver le GC en production révélerait le 3× obtenu au micro-benchmark"],"answer":0,"explanation":"C'est le décalage MICRO vs MACRO. Un micro-benchmark mesure une fonction ISOLÉE dans des conditions idéalisées (données chaudes en cache, pas de contention, pas d'I/O concurrente, entrées non représentatives). Un gain de 3× sur cette fonction ne se traduit en gain global QUE si la fonction pèse significativement dans le temps total (loi d'Amdahl) ET si les conditions de prod ressemblent au benchmark. Ici, l'absence d'amélioration suggère typiquement que la fonction n'était pas sur le hot path réel, ou que le vrai goulot est ailleurs (I/O, contention, cache froid). La démarche rigoureuse (Gregg, 'active benchmarking') exige de valider avec un macro-benchmark représentatif et de profiler la charge RÉELLE avant de conclure. Distracteurs : les micro-benchmarks ne sont pas 'à proscrire' (ils sont utiles pour comparer des implémentations, à condition de savoir ce qu'ils mesurent) ; la 'dévectorisation au déploiement' et le 'GC qui masque tout' sont des explications spéculatives et non génériques, contredisant l'analyse par le hot path/Amdahl qui est la cause première et la plus probable.","tags":["benchmarking","micro-macro","amdahl","hot-path"],"optionNotes":["La fonction n'était pas sur le hot path réel (Amdahl) ; le micro-benchmark ignore le contexte prod.","Faux : les micro-benchmarks restent utiles pour comparer des implémentations.","Faux : explication spéculative ; réaligner les flags ne « restaure pas toujours » le gain.","Faux : hypothèse non générique ; l'analyse par hot path/Amdahl est plus probable."]},
  {"id":"perf-copie-superflue-en-passant-par-valeur-c","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Copie superflue en passant par valeur (C++)","company":"Scott Meyers, Effective C++ Item 20 (pass by reference-to-const) ; The Rust/C++ perf books (avoid copies)","language":"c","code":"// C++ compile en C-like ; std::vector<std::string>\nsize_t total_len(std::vector<std::string> lines) {\n    size_t n = 0;\n    for (std::string s : lines) {\n        n += s.size();\n    }\n    return n;\n}","question":"Cette fonction fait au moins deux copies coûteuses évitables. Lesquelles ?","options":["Le paramètre `std::vector<std::string> lines` est pris PAR VALEUR (copie tout le vecteur et chaque string) ; et `for (std::string s : lines)` copie CHAQUE string à chaque itération. Passer `const std::vector<std::string>&` et itérer `const std::string& s` supprime les deux","`n += s.size()` copie la string dans un registre avant d'appeler size() ; il faut utiliser `s.length()` qui opère par référence, et le paramètre par valeur est en réalité optimisé par le compilateur via RVO","Le retour `size_t` par valeur copie un objet lourd ; retourner par référence `size_t&` évite la copie, tandis que le paramètre vecteur est déjà passé par référence implicite en C++","`for (std::string s : lines)` utilise un itérateur qui déréférence deux fois par élément ; passer à une boucle indexée `lines[i]` supprime la double indirection, seule copie réelle du code"],"answer":0,"explanation":"Deux copies profondes évitables. (1) Le paramètre `std::vector<std::string> lines` est pris PAR VALEUR : appeler la fonction COPIE l'intégralité du vecteur ET chacune de ses strings (allocations sur le tas) — inutile puisqu'on ne fait que lire. Correction : `const std::vector<std::string>& lines`. (2) La boucle `for (std::string s : lines)` déclare `s` par VALEUR : à chaque itération, la string est COPIÉE dans `s` (nouvelle allocation). Correction : `for (const std::string& s : lines)` pour itérer par référence constante. Ensemble, on passe de O(taille totale des données) copies à zéro copie. Distracteurs : `s.size()` ne copie rien dans un registre (retourne un size_t), `length()` est un alias identique, et il n'y a pas de RVO sur un paramètre ; retourner un `size_t` par valeur est trivial (un mot) et le retourner par référence serait dangereux (dangling) ; la boucle par index ne réglerait PAS la copie si on écrivait encore `std::string s = lines[i]`. Le vrai coût est le passage/itération par valeur.","tags":["cpp","copy","pass-by-reference","allocation"],"optionNotes":["Le paramètre par valeur copie tout le vecteur, et for par valeur copie chaque string ; passer par const&.","Faux : size() ne copie rien, length() est un alias, et il n'y a pas de RVO sur un paramètre.","Faux : retourner un size_t est trivial ; le vecteur n'est pas passé par référence implicite.","Faux : une boucle indexée ne règle pas la copie si l'on écrit encore std::string s = lines[i]."]},
  {"id":"perf-bloom-filter-quand-accepter-les-faux-pos","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Bloom filter : quand accepter les faux positifs","company":"Bloom (1970) ; Brendan Gregg / cache admission ; CLRS-adjacent probabilistic data structures","question":"Avant une requête coûteuse (lookup disque/réseau), on interpose un filtre de Bloom en mémoire. Quel est le compromis fondamental et un cas où il fait GAGNER de la performance ?","options":["Un Bloom filter répond « peut-être présent » (avec un taux de faux positifs réglable) ou « certainement absent » (jamais de faux négatif) en O(k) et peu de mémoire ; il évite la requête coûteuse pour les absences certaines, gain net quand beaucoup de lookups portent sur des clés inexistantes","Un Bloom filter garantit zéro faux positif et zéro faux négatif au prix d'une mémoire O(n) équivalente à un hash set ; son intérêt est uniquement une meilleure localité de cache que le hash set","Un Bloom filter peut retourner des faux négatifs mais jamais de faux positifs, ce qui le rend sûr pour éviter des écritures inutiles ; il faut donc l'utiliser côté écriture et non côté lecture","Un Bloom filter stocke les clés compressées et permet de les RÉCUPÉRER en O(1), remplaçant complètement le cache tout en divisant la mémoire par le taux de faux positifs choisi"],"answer":0,"explanation":"Un filtre de Bloom est une structure probabiliste compacte (bitset + k fonctions de hash). Propriété clé asymétrique : il ne produit JAMAIS de faux négatif (« certainement absent » est fiable) mais PEUT produire des faux positifs (« peut-être présent »), avec un taux réglable selon la taille du bitset et k. Usage gagnant : le placer AVANT une opération coûteuse (lecture disque, RPC, lookup DB). Si le filtre dit « certainement absent », on SAUTE la requête coûteuse — gain massif quand une grande part des lookups concerne des clés inexistantes (ex. LSM-tree/SSTables sautant les fichiers ne contenant pas la clé, cache d'admission). En cas de faux positif on paie la requête pour rien, mais sans jamais manquer une vraie présence. Distracteurs : le Bloom filter N'A PAS zéro faux positif (c'est tout son intérêt/compromis) ; il n'a JAMAIS de faux négatif (l'option 3 inverse la propriété, ce qui le rendrait dangereux en lecture) ; il ne STOCKE PAS les clés et ne permet PAS de les récupérer (pas d'énumération/récupération). Le compromis est : mémoire minuscule contre un taux contrôlé de faux positifs.","tags":["bloom-filter","probabiliste","cache","io"],"optionNotes":["Un Bloom filter ne ment jamais sur l'absence : il saute la requête coûteuse pour les clés absentes.","Faux : le Bloom filter A des faux positifs (c'est tout son compromis mémoire).","Faux : il inverse la propriété ; jamais de faux négatif, ce qui le rend sûr en lecture.","Faux : il ne stocke pas les clés et ne permet pas de les récupérer."]},
  {"id":"perf-verrou-tenu-pendant-une-i-o","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Verrou tenu pendant une I/O","company":"Java Concurrency in Practice ch. 11 (narrow lock scope) ; Brendan Gregg, Systems Performance (lock contention)","language":"java","code":"class Cache {\n    private final Map<String, byte[]> map = new HashMap<>();\n    synchronized byte[] get(String key) {\n        byte[] v = map.get(key);\n        if (v == null) {\n            v = fetchFromNetwork(key); // ~50 ms d'I/O reseau\n            map.put(key, v);\n        }\n        return v;\n    }\n}","question":"Sous forte concurrence, ce cache sérialise tous les threads. Quelle est l'erreur de conception ?","options":["Le verrou (`synchronized`) est tenu pendant l'I/O réseau de ~50 ms : tous les autres threads sont bloqués pendant ce temps même pour des clés déjà en cache. Il faut réduire la portée du verrou (verrouiller la lecture/écriture de la map, pas l'I/O) et gérer le doublon de fetch séparément","`HashMap` n'est pas thread-safe donc le `synchronized` est insuffisant ; remplacer par `ConcurrentHashMap` supprime à lui seul toute la contention et rend le fetch non bloquant","`fetchFromNetwork` devrait être appelé en dehors de la classe ; l'erreur est un problème d'architecture en couches, sans impact réel sur la performance du verrou","Le `synchronized` sur la méthode crée un verrou par instance qui entre en deadlock avec le thread réseau ; utiliser un verrou statique global résout la sérialisation"],"answer":0,"explanation":"L'erreur est la PORTÉE du verrou : `synchronized` couvre TOUTE la méthode, y compris l'appel réseau `fetchFromNetwork` (~50 ms). Tant qu'un thread attend le réseau en tenant le moniteur, TOUS les autres threads — même ceux voulant lire une clé DÉJÀ présente en cache (opération microseconde) — sont bloqués. Le cache censé accélérer sérialise tout le monde derrière la plus lente des I/O. Bonne pratique (réduire la portée du verrou) : verrouiller uniquement les accès à la structure (get puis put), effectuer l'I/O réseau HORS du verrou, et gérer proprement le cas où deux threads fetchent la même clé en parallèle (ex. valeur = future/CompletableFuture stockée sous verrou, ou `computeIfAbsent` avec les précautions idoines). Distracteurs : passer à `ConcurrentHashMap` aide pour la structure mais NE RÈGLE PAS le fait de bloquer pendant l'I/O si on garde une section critique large — et `computeIfAbsent` de ConcurrentHashMap exécuterait justement le fetch SOUS le verrou du bucket, reproduisant le problème ; ce n'est pas un simple souci d'architecture ni un deadlock (il n'y en a pas), et un verrou global aggraverait la sérialisation.","tags":["lock-scope","contention","io","concurrence"],"optionNotes":["Le synchronized couvre l'I/O réseau (~50 ms) : réduire la portée au seul accès à la map.","Faux : ConcurrentHashMap.computeIfAbsent exécuterait le fetch sous verrou, même problème.","Faux : ce n'est pas qu'une question d'architecture ; l'impact perf est réel.","Faux : il n'y a pas de deadlock ; un verrou global aggraverait la sérialisation."]},
  {"id":"perf-cout-du-context-switch-et-granularite-de","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Coût du context switch et granularité des tâches","company":"Brendan Gregg, Systems Performance ch. 6 (scheduling, context switch cost) ; parallélisme à grain fin","question":"Vous parallélisez une boucle en soumettant CHAQUE itération (travail ~200 ns) comme une tâche séparée à un pool de threads. Le résultat est plus lent que la version séquentielle. Pourquoi ?","options":["Le grain est trop FIN : le coût de soumission/planification d'une tâche (mise en file, réveil de thread, context switch ~1-5 µs) dépasse largement les ~200 ns de travail utile, dominant totalement. Il faut regrouper les itérations en lots (chunking) pour amortir l'overhead de scheduling","Le pool de threads utilise un seul cœur par défaut ; il faut augmenter explicitement le nombre de threads au-delà du nombre de cœurs physiques pour que la parallélisation apporte un gain sur des tâches courtes","Chaque tâche courte déclenche un cycle de garbage collection en la libérant, et l'accumulation de ces GC dépasse le coût du travail ; augmenter la taille du tas supprime ce surcoût","Les tâches de 200 ns sont trop rapides pour que le prédicteur de branche du scheduler apprenne leur motif, forçant un flush du pipeline à chaque tâche qui annule le gain parallèle"],"answer":0,"explanation":"Problème de GRANULARITÉ (grain trop fin). Soumettre une tâche a un COÛT FIXE : allocation/mise en file de la tâche, synchronisation de la queue, éventuel réveil d'un thread endormi, et context switches (~1-5 µs chacun, plus la pollution de cache associée). Quand le travail utile par tâche est ~200 ns, cet overhead de plusieurs µs par tâche l'écrase d'un ou deux ordres de grandeur : on passe son temps à ordonnancer, pas à calculer. La solution est le CHUNKING/batching : regrouper des milliers d'itérations par tâche pour que le travail utile amortisse le coût de scheduling (c'est ce que font les splitters de parallelStream, Rayon, TBB avec un grain minimal). Distracteurs : un ThreadPool/ForkJoinPool utilise par défaut plusieurs threads (≈ nb de cœurs), pas un seul, et sur-souscrire n'aiderait pas des tâches trop courtes ; les petites tâches ne déclenchent pas un GC chacune ; le 'prédicteur de branche du scheduler' est une invention. Le principe : le grain parallèle doit être assez gros pour amortir l'overhead de coordination.","tags":["granularite","context-switch","parallelisme","batching"],"optionNotes":["Le coût de soumission/context-switch (~1-5 µs) dépasse les 200 ns utiles ; regrouper en lots (chunking).","Faux : un pool utilise par défaut plusieurs threads ; sur-souscrire n'aiderait pas.","Faux : les petites tâches ne déclenchent pas un GC chacune.","Faux : le « prédicteur de branche du scheduler » est une invention."]},
  {"id":"perf-cache-invalide-trop-large-over-invalidat","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Cache invalidé trop large (over-invalidation)","company":"Brendan Gregg, Systems Performance ch. 2 (cache efficacité) ; patterns cache-aside / invalidation ciblée","language":"python","code":"cache = {}\n\ndef get_user_profile(uid):\n    if uid not in cache:\n        cache[uid] = db_load_profile(uid)   # requete couteuse\n    return cache[uid]\n\ndef on_any_user_write(uid):\n    cache.clear()   # invalider apres une ecriture","question":"Fonctionnellement correct, ce cache a un défaut de performance sous charge d'écriture. Lequel ?","options":["`cache.clear()` invalide TOUT le cache à chaque écriture d'UN seul utilisateur : sous un flux d'écritures, le hit ratio s'effondre car des milliers d'entrées non concernées sont jetées puis rechargées ; il faut invalider ciblé : `cache.pop(uid, None)`","`cache.clear()` ne libère pas réellement la mémoire du dict en Python (il garde la capacité allouée), donc le cache grossit indéfiniment malgré l'invalidation ; utiliser `cache = {}` corrige la fuite","Le test `uid not in cache` recalcule le hash de uid à chaque appel, ce qui rend le get O(n) sous forte charge ; mettre en cache le hash de uid résout le ralentissement","`db_load_profile` est appelé sous le GIL, sérialisant tous les threads de lecture ; libérer le GIL pendant la requête est le vrai correctif de performance"],"answer":0,"explanation":"Le défaut est l'OVER-INVALIDATION : `cache.clear()` vide l'INTÉGRALITÉ du cache alors qu'une seule entrée (celle de `uid`) est devenue périmée. Sous une charge d'écriture soutenue, chaque write jette des milliers d'entrées valides et non concernées, qui devront toutes être rechargées depuis la DB au prochain accès → le hit ratio s'effondre et on rappelle la requête coûteuse en boucle, annihilant l'intérêt du cache. Correction : invalidation CIBLÉE de la seule clé modifiée : `cache.pop(uid, None)`. C'est l'illustration que 'l'invalidation de cache est difficile' : trop large = perte d'efficacité, trop étroite/oubliée = données périmées. Distracteurs : `cache.clear()` vide bien le contenu (la capacité résiduelle du dict est négligeable, pas une 'fuite') ; le hash d'un int/str est O(1) (et caché pour les str) — pas un O(n) ; le GIL n'est pas le sujet ici (le problème existe même mono-thread). Le principe : invalider le minimum nécessaire.","tags":["cache-invalidation","hit-ratio","python","over-invalidation"],"optionNotes":["cache.clear() jette tout à chaque write d'un seul uid : invalider ciblé avec cache.pop(uid, None).","Faux : clear() vide bien le contenu ; la capacité résiduelle n'est pas une fuite.","Faux : le hash d'un int/str est O(1) (et caché pour les str).","Faux : le GIL n'est pas en cause ; le problème existe même en mono-thread."]},
  {"id":"perf-use-method-diagnostiquer-un-goulot-syste","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"USE Method : diagnostiquer un goulot système","company":"Brendan Gregg, Systems Performance 2e éd., ch. 2 (The USE Method)","question":"La méthode USE de Brendan Gregg guide le diagnostic de goulots. Que recouvrent exactement U, S, E et quel est leur ordre d'application pour une ressource ?","options":["Utilization (temps occupé / temps de non-idle), Saturation (travail en attente que la ressource ne peut traiter, ex. longueur de file), Errors (erreurs matérielles/logicielles) ; pour chaque ressource on vérifie erreurs, utilisation, puis saturation afin de localiser le composant sous pression","Usage (mémoire consommée), Speed (débit instantané), Efficiency (ratio travail utile) ; on les mesure simultanément sur le CPU uniquement, la mémoire et l'I/O étant hors du périmètre de la méthode","Uptime (disponibilité), Scalability (capacité à monter en charge), Elasticity (adaptation dynamique) ; c'est une méthode de capacity planning appliquée avant tout déploiement, pas de diagnostic en temps réel","Utilization, Stability, Endurance ; on identifie d'abord la ressource la plus utilisée puis on teste sa stabilité sous charge prolongée, en ignorant les files d'attente qui relèvent d'une autre méthode"],"answer":0,"explanation":"La méthode USE de Gregg s'applique à CHAQUE ressource (CPU, mémoire, disques, interfaces réseau, bus…) et vérifie trois métriques : UTILIZATION (proportion de temps où la ressource est occupée/non-idle, ou fraction de capacité utilisée), SATURATION (degré de travail en attente que la ressource ne peut absorber immédiatement — typiquement la longueur des files d'attente : run queue, I/O queue, swap), et ERRORS (comptes d'erreurs, souvent le plus rapide à écarter et pouvant expliquer des dégradations). La démarche : pour chaque ressource, cocher errors → utilization → saturation, afin d'identifier systématiquement le composant réellement sous pression avant d'optimiser à l'aveugle. Distracteurs : les trois autres définitions sont des inventions plausibles mais fausses (Gregg définit précisément Utilization/Saturation/Errors) ; la méthode couvre TOUTES les ressources, pas seulement le CPU, et c'est une méthode de diagnostic en temps réel, pas seulement du capacity planning. C'est le socle du 'mesurer avant d'optimiser' au niveau système.","tags":["use-method","profiling","systeme","diagnostic"],"optionNotes":["USE = Utilization, Saturation (files d'attente), Errors ; vérifiées sur chaque ressource.","Faux : ce ne sont pas Usage/Speed/Efficiency, et la méthode couvre toutes les ressources.","Faux : USE n'est pas Uptime/Scalability/Elasticity ; c'est un diagnostic temps réel.","Faux : la saturation (files d'attente) est au cœur de la méthode, pas ignorée."]},
  {"id":"perf-reallocation-implicite-slice-go-sans-cap","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Réallocation implicite : slice Go sans capacité","company":"Go blog « Arrays, slices » ; The Go Programming Language (Donovan/Kernighan) ch. 4 ; growth amorti","language":"go","code":"func collectIDs(rows []Row) []int64 {\n    var ids []int64                 // nil slice\n    for _, r := range rows {        // 10_000_000 rows\n        ids = append(ids, r.ID)\n    }\n    return ids\n}","question":"La taille finale est connue (len(rows)). Quelle optimisation simple évite des réallocations répétées, et pourquoi ?","options":["Pré-allouer avec `ids := make([]int64, 0, len(rows))` : sinon `append` double périodiquement la capacité, recopiant tout le backing array à chaque croissance (~log₂(n) réallocations et O(n) copies au total). Réserver la capacité exacte supprime toutes ces copies","Utiliser `ids := make([]int64, len(rows))` puis `append` : la longueur initiale garantit qu'append n'alloue jamais, mais il faut indexer par position pour ne pas décaler les éléments","Remplacer `append` par une écriture indexée `ids[i] = r.ID` sur une slice nil fonctionne sans allocation car Go alloue paresseusement au premier accès, évitant tout redimensionnement","Le vrai coût est le `range rows` qui copie chaque `Row` (structure large) dans `r` ; pré-allouer ids ne change rien tant qu'on n'itère pas avec un index sur rows"],"answer":0,"explanation":"Partir d'une slice `nil` et faire `append` répété la fait croître par redoublements géométriques de la capacité : à chaque dépassement, Go alloue un NOUVEAU backing array plus grand et RECOPIE tous les éléments existants. Pour n=10M, cela fait ~log₂(n) réallocations et O(n) copies cumulées (plus la pression GC des anciens tableaux). Puisque la taille finale est connue, `ids := make([]int64, 0, len(rows))` réserve la CAPACITÉ exacte dès le départ : les `append` suivants n'entraînent AUCUNE réallocation ni copie. Distracteurs : `make([]int64, len(rows))` crée une slice de LONGUEUR len(rows) (déjà remplie de zéros), et `append` AJOUTERAIT à la fin après ces zéros — bug de doublement de taille (il faudrait indexer `ids[i]=`, pas append) ; on ne peut PAS écrire `ids[i]` sur une slice nil (panic index out of range, pas d'allocation paresseuse) ; l'option 4 est en partie vraie (copier une Row large dans `r` a un coût, réductible via index) mais ce n'est pas ce que la question demande, et elle nie à tort le gain de la pré-allocation d'ids. Le bon geste : réserver la capacité connue.","tags":["go","slice","preallocation","amortissement"],"optionNotes":["make([]int64, 0, len(rows)) réserve la capacité exacte ; sinon append double et recopie (~log₂ n fois).","Faux : make(..., len) crée une slice déjà remplie de zéros ; append doublerait la taille.","Faux : écrire ids[i] sur une slice nil provoque un index out of range, pas d'alloc paresseuse.","Faux : préallouer ids supprime bien les réallocations (la copie de Row est un point séparé)."]},
  {"id":"perf-latency-numbers-mutex-vs-main-memory-vs-","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Latency numbers : mutex vs main memory vs branch mispredict","company":"Jeff Dean, « Latency Numbers Every Programmer Should Know » ; Brendan Gregg, Systems Performance ch. 2","question":"Classez du plus RAPIDE au plus lent : (a) lock/unlock d'un mutex non contendu, (b) référence mémoire principale, (c) mauvaise prédiction de branche, (d) référence cache L1.","options":["L1 (~0,5 ns) < branch mispredict (~5 ns) < mutex (~25 ns) < mémoire principale (~100 ns)","L1 (~0,5 ns) < mémoire principale (~100 ns) < branch mispredict (~5 ns) < mutex (~25 ns)","branch mispredict (~5 ns) < L1 (~0,5 ns) < mémoire principale (~100 ns) < mutex (~25 ns)","L1 (~0,5 ns) < mutex (~25 ns) < branch mispredict (~5 ns) < mémoire principale (~100 ns)"],"answer":0,"explanation":"Repères de Jeff Dean : référence L1 ≈ 0,5 ns ; mauvaise prédiction de branche ≈ 5 ns ; lock/unlock d'un mutex NON contendu ≈ 25 ns ; référence à la mémoire principale ≈ 100 ns. L'ordre correct est donc L1 < branch mispredict < mutex < mémoire principale. Point contre-intuitif crucial : une référence à la mémoire principale (~100 ns) est PLUS LENTE qu'un lock/unlock de mutex non contendu (~25 ns) — d'où l'importance de la localité de cache. Les options 2, 3 et 4 mélangent ces valeurs : la 2 place à tort la mémoire avant le mispredict/mutex, la 3 met le mispredict avant le L1 et désordonne, la 4 place le mutex (25 ns) avant le mispredict (5 ns). Connaître ces ordres de grandeur permet de savoir où se trouve réellement le coût (un cache miss coûte ~200× un hit L1, ~20× un mispredict de branche).","tags":["latency-numbers","cache","mutex","branch-mispredict"],"optionNotes":["Ordre de Jeff Dean : L1 0,5 ns < mispredict 5 ns < mutex 25 ns < mémoire 100 ns.","Faux : place à tort la mémoire (100 ns) avant le mispredict et le mutex.","Faux : met le mispredict avant le L1 et désordonne le reste.","Faux : place le mutex (25 ns) avant le mispredict (5 ns)."]},
  {"id":"perf-serialisation-json-dans-une-boucle-de-lo","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Sérialisation JSON dans une boucle de log chaude","company":"Brendan Gregg, Systems Performance ch. 5 (CPU hot path) ; observabilité / logging à faible overhead (lazy logging)","language":"python","code":"import logging, json\nlog = logging.getLogger(__name__)\n\ndef handle(request, ctx):\n    # niveau effectif : INFO (DEBUG desactive en prod)\n    log.debug(\"ctx=%s\", json.dumps(ctx.to_dict()))\n    process(request)","question":"En production (niveau INFO), cette ligne de log DEBUG coûte quand même cher. Pourquoi, et quel est le correctif ?","options":["`json.dumps(ctx.to_dict())` est ÉVALUÉ avant l'appel à `log.debug` (arguments Python évalués en amont), donc la sérialisation coûteuse s'exécute même si DEBUG est filtré. Utiliser un guard `if log.isEnabledFor(logging.DEBUG)` ou un objet à `__str__` paresseux évite ce calcul","`logging.getLogger(__name__)` recrée un logger à chaque appel de module, et l'accumulation de handlers ralentit chaque log ; mettre le logger en variable globale unique résout le surcoût","`log.debug` écrit sur disque de façon synchrone même quand le niveau est INFO, car le filtrage de niveau se fait après l'écriture ; passer à un handler asynchrone supprime la latence I/O","`ctx.to_dict()` acquiert un verrou global sur ctx qui sérialise les requêtes concurrentes ; c'est la contention, non la sérialisation JSON, qui domine le coût en production"],"answer":0,"explanation":"Piège d'évaluation des arguments : en Python (comme dans la plupart des langages), les ARGUMENTS sont évalués AVANT l'appel de fonction. Ici, `json.dumps(ctx.to_dict())` est calculé (sérialisation JSON potentiellement coûteuse + construction du dict) AVANT même que `log.debug` ne décide, en interne, de jeter le message parce que le niveau effectif est INFO. On paie donc tout le coût de sérialisation à chaque requête, en pure perte. Correctifs : (1) garder l'appel derrière `if log.isEnabledFor(logging.DEBUG):`, (2) passer un objet dont `__str__` fait la sérialisation paresseusement (évalué seulement si le message est réellement émis), ou (3) laisser le formatage au logger avec des champs bruts. Distracteurs : `getLogger(__name__)` retourne le MÊME logger (cache interne), pas de nouveaux handlers ; le filtrage de niveau se fait AVANT l'écriture (pas après) donc pas d'I/O synchrone ici — mais le calcul de l'argument, lui, a déjà eu lieu ; il n'y a pas de verrou global sur ctx. Le fond : ne pas payer la construction d'un message de log qui sera filtré.","tags":["logging","lazy-evaluation","hot-path","python"],"optionNotes":["Les arguments sont évalués avant l'appel : json.dumps s'exécute même si DEBUG est filtré (guard isEnabledFor).","Faux : getLogger(__name__) renvoie le même logger caché, sans handlers accumulés.","Faux : le filtrage de niveau se fait avant l'écriture ; pas d'I/O synchrone ici.","Faux : il n'y a pas de verrou global sur ctx."]},
  {"id":"perf-traduction-d-adresse-tlb-miss-et-huge-pa","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Traduction d'adresse : TLB miss et huge pages","company":"Brendan Gregg, Systems Performance 2e éd., ch. 7 (Memory, TLB, huge pages) ; « What Every Programmer Should Know About Memory »","question":"Une application manipulant plusieurs Go de données à accès dispersé subit de nombreux TLB misses. En quoi les huge pages (2 Mo au lieu de 4 Ko) peuvent-elles aider, et quel est le risque ?","options":["Une entrée TLB de 2 Mo couvre 512× plus de mémoire qu'une page de 4 Ko : à TLB de taille fixe, on cartographie beaucoup plus d'espace, réduisant les misses (chacun coûtant un page-walk de plusieurs accès mémoire). Risque : gaspillage mémoire par fragmentation interne et pression accrue si l'usage réel est épars","Les huge pages augmentent la taille du cache L1 de données proportionnellement à la taille de page, ce qui réduit les défauts de cache ; le seul risque est une latence de démarrage plus longue à l'allocation initiale","Les huge pages désactivent la pagination sur disque pour la zone concernée, éliminant tout swap ; le risque est de perdre la protection mémoire par page, exposant l'application à des corruptions","Les huge pages permettent au CPU d'adresser directement la mémoire physique sans passer par la MMU, supprimant totalement le besoin de TLB ; le risque est l'incompatibilité avec la mémoire virtuelle par processus"],"answer":0,"explanation":"Le TLB (Translation Lookaside Buffer) cache les traductions adresse virtuelle→physique. Il a un nombre FIXE et limité d'entrées. Avec des pages de 4 Ko, N entrées couvrent N×4 Ko ; avec des HUGE PAGES de 2 Mo, les MÊMES N entrées couvrent N×2 Mo, soit 512× plus de mémoire cartographiée simultanément → beaucoup moins de TLB misses. Un TLB miss est coûteux car il déclenche un PAGE WALK (parcours de la table des pages, plusieurs accès mémoire potentiellement en cache miss) avant de reprendre l'accès. Pour des données de plusieurs Go à accès dispersé, réduire les TLB misses via huge pages peut apporter un gain notable. RISQUE : fragmentation interne (une huge page allouée mais partiellement utilisée gaspille jusqu'à ~2 Mo), pression mémoire accrue et allocation plus difficile (besoin de blocs physiques contigus). Distracteurs : les huge pages ne changent PAS la taille du cache L1 (indépendant de la taille de page) ; elles ne 'désactivent pas le swap' ni ne suppriment la protection par page ; le CPU utilise TOUJOURS la MMU/TLB avec huge pages (elles ne 'suppriment pas le TLB', elles le rendent plus efficace). Le mécanisme réel : couverture TLB étendue.","tags":["tlb","huge-pages","memoire","mmu"],"optionNotes":["Une entrée TLB de 2 Mo couvre 512× plus qu'une de 4 Ko → moins de misses ; risque : fragmentation interne.","Faux : les huge pages ne changent pas la taille du cache L1 (indépendant de la page).","Faux : elles ne désactivent pas le swap ni ne suppriment la protection par page.","Faux : le CPU utilise toujours la MMU/TLB avec huge pages."]},
  {"id":"perf-recherche-associative-par-scan-dans-une-","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Recherche associative par scan dans une boucle SQL applicative","company":"Bentley, Programming Pearls ch. 8 (precompute lookup) ; Use The Index, Luke! (jointure vs scan applicatif)","language":"sql","code":"-- pseudo-boucle applicative generant N requetes\n-- pour chaque produit d'une commande (jusqu'a 500 lignes) :\nSELECT price\nFROM price_history\nWHERE product_id = :pid\nORDER BY effective_date DESC\nLIMIT 1;\n-- price_history : 40M lignes, index sur (product_id, effective_date)","question":"Chaque requête est indexée et rapide, mais traiter une commande de 500 lignes est lent. Quel est le problème d'ensemble et l'approche correcte ?","options":["C'est un N+1 applicatif : 500 requêtes = 500 allers-retours réseau/planification, chacun payant sa latence même si l'index est parfait. Récupérer tous les prix en UNE requête (WHERE product_id IN (...) avec DISTINCT ON / window function pour le dernier par produit) amortit le coût fixe","L'index sur (product_id, effective_date) est mal ordonné : il faut le recréer en (effective_date, product_id) pour que le ORDER BY DESC LIMIT 1 devienne O(1), ce qui supprimera la lenteur sans changer le nombre de requêtes","Le ORDER BY DESC force un tri complet des 40M lignes à chaque requête ; ajouter un index descendant explicite sur effective_date transforme chaque requête et rend la boucle négligeable","Le LIMIT 1 empêche le cache de plan de la base de réutiliser la requête préparée ; supprimer LIMIT et filtrer côté application permet la réutilisation du plan et accélère l'ensemble"],"answer":0,"explanation":"Chaque requête individuelle est bien optimisée (l'index composite (product_id, effective_date) permet un accès direct + LIMIT 1 sans tri global). Le problème est GLOBAL : émettre 500 requêtes SÉPARÉES pour une seule commande, c'est un N+1 applicatif — on paie 500 fois la latence d'aller-retour réseau, la planification et le coût fixe par requête, même si chacune est rapide. La bonne approche est de BATCHER : une seule requête récupérant le dernier prix de tous les produits concernés, par ex. `SELECT DISTINCT ON (product_id) product_id, price FROM price_history WHERE product_id IN (:ids) ORDER BY product_id, effective_date DESC` (PostgreSQL), ou une window function `ROW_NUMBER() OVER (PARTITION BY product_id ORDER BY effective_date DESC)`. Distracteurs : l'index (product_id, effective_date) est DÉJÀ optimal pour ce prédicat + tri (product_id en tête = accès direct, puis effective_date pour le LIMIT 1) — l'inverser serait pire ; il n'y a PAS de tri des 40M lignes (l'index évite le tri) ; le LIMIT 1 n'empêche pas la réutilisation du plan. Le fond : amortir le coût fixe des allers-retours en batchant.","tags":["n+1","batching","sql","index"],"optionNotes":["500 requêtes = 500 allers-retours ; récupérer tous les prix en une requête (IN + DISTINCT ON) amortit.","Faux : (product_id, effective_date) est déjà optimal ; l'inverser serait pire.","Faux : l'index évite tout tri des 40M lignes.","Faux : le LIMIT 1 n'empêche pas la réutilisation du plan."]},
  {"id":"perf-std-unordered-map-vs-std-map-quand-le-o-","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"std::unordered_map vs std::map : quand le O(1) perd","company":"Scott Meyers / cppreference (unordered_map buckets, node-based) ; Chandler Carruth talks (data structures & cache)","question":"En théorie `std::unordered_map` (O(1) amorti) bat `std::map` (arbre, O(log n)). Pourtant, pour de PETITES tables avec accès fréquents, `std::map` ou même un `std::vector` trié gagne parfois. Pourquoi ?","options":["`unordered_map` est node-based : chaque entrée est un nœud alloué séparément, dispersé en mémoire → pointer chasing et cache misses. Pour de petites tables tenant en cache, un vector trié contigu (recherche dichotomique, données préchargées) offre une bien meilleure localité malgré un O(log n)","`unordered_map` recalcule le facteur de charge à chaque accès et déclenche un rehash complet dès 50 % de remplissage, ce qui le rend plus lent que `std::map` sur toute table de moins de 1000 éléments","`std::map` utilise un arbre B optimisé pour le cache (nœuds de 64 o) alors que `unordered_map` stocke un seul élément par ligne de cache ; c'est pourquoi map est toujours plus rapide en lecture séquentielle","La fonction de hachage de `unordered_map` est cryptographique par défaut (SHA) pour éviter le hash flooding, et son coût par accès dépasse la comparaison d'un arbre pour les petites tables"],"answer":0,"explanation":"La théorie asymptotique ignore les CONSTANTES et la LOCALITÉ. `std::unordered_map` est spécifié comme NODE-BASED : chaque paire est un nœud alloué individuellement sur le tas, chaîné dans son bucket. Les nœuds sont donc dispersés en mémoire → suivre les buckets/chaînes = pointer chasing et cache misses coûteux (~100 ns chacun). Pour de PETITES tables (qui tiennent en cache) et des accès fréquents, un `std::vector` trié parcouru en recherche dichotomique est CONTIGU : le prefetcher précharge, chaque comparaison est un hit L1, et le O(log n) sur peu d'éléments est minuscule. Résultat : la structure contiguë bat souvent la table de hachage dispersée en pratique, malgré la meilleure complexité asymptotique de cette dernière. Distracteurs : le rehash ne se déclenche pas à 50 % ni à chaque accès (facteur de charge max par défaut = 1.0, rehash occasionnel amorti) ; `std::map` est un arbre rouge-noir node-based (PAS un B-tree cache-friendly — c'est justement une autre structure dispersée), donc 'toujours plus rapide en séquentiel' est faux ; la fonction de hachage standard n'est PAS cryptographique. La leçon : mesurer, et privilégier la contiguïté pour les petites structures chaudes.","tags":["cpp","cache","unordered-map","data-structure"],"optionNotes":["unordered_map est node-based (nœuds dispersés) ; un vector trié contigu offre une meilleure localité.","Faux : le rehash ne se déclenche pas à 50 % (load factor max = 1.0, amorti).","Faux : std::map est un arbre rouge-noir node-based, pas un B-tree cache-friendly.","Faux : la fonction de hachage standard n'est pas cryptographique."]},
  {"id":"perf-recompilation-d-un-template-regex-cote-d","type":"find-error","domain":"software","topic":"performance","difficulty":"senior","title":"Recompilation d'un template regex côté DOM (JS)","company":"Bentley, Programming Pearls ch. 8 (hoist invariants) ; MDN (layout thrashing) ; hot path front-end","language":"javascript","code":"function highlight(rows, term) {\n  const container = document.getElementById('list');\n  for (const row of rows) {          // 10_000 lignes\n    const re = new RegExp(term, 'gi');\n    const el = document.createElement('div');\n    el.innerHTML = row.text.replace(re, m => `<mark>${m}</mark>`);\n    container.appendChild(el);\n    const h = container.offsetHeight;  // pour \"forcer le layout\"\n  }\n}","question":"Deux problèmes de performance distincts se cachent ici. Lesquels ?","options":["(1) `new RegExp(term,'gi')` recompile le motif invariant à chaque itération (à hisser hors boucle) ; (2) lire `container.offsetHeight` après chaque `appendChild` force un reflow synchrone à chaque tour (layout thrashing), au lieu d'un seul reflow en batchant les insertions via un DocumentFragment","(1) `document.getElementById` est appelé dans la boucle et re-parcourt le DOM à chaque fois ; (2) `innerHTML` est plus lent que `textContent`, donc le seul correctif est de remplacer innerHTML par des nœuds texte créés manuellement","(1) `row.text.replace` crée une copie de chaîne, saturant le GC ; (2) `appendChild` déclenche un garbage collect à chaque insertion, à éviter en réutilisant un pool d'éléments div préalloués","(1) La regex avec flag 'g' garde un état lastIndex entre appels, corrompant les correspondances ; (2) `offsetHeight` renvoie une valeur mise en cache donc n'a aucun coût, le seul vrai problème étant l'état de la regex"],"answer":0,"explanation":"Deux problèmes distincts. (1) INVARIANT DE BOUCLE : `new RegExp(term, 'gi')` compile le motif à CHAQUE itération alors que `term` ne change pas — à hisser hors de la boucle (`const re = new RegExp(term,'gi')` une fois ; attention à réinitialiser lastIndex si on réutilise le même objet avec 'g', ou recréer une fois suffit ici car .replace gère le parcours). (2) LAYOUT THRASHING : lire une propriété géométrique comme `offsetHeight` APRÈS une mutation du DOM (`appendChild`) force le navigateur à recalculer la mise en page (reflow) SYNCHRONEMENT à chaque tour — 10 000 reflows au lieu d'un. Le correctif est de batcher : construire les éléments dans un `DocumentFragment` (hors du document vivant), puis faire un seul `container.appendChild(fragment)` → un seul reflow. Distracteurs : `getElementById` est appelé UNE fois hors boucle (pas le problème) ; le vrai problème n'est pas innerHTML vs textContent ni un GC par appendChild ; `offsetHeight` n'est PAS mis en cache (sa lecture après mutation force justement le reflow — l'option 4 inverse la réalité). Les deux vrais coupables sont l'invariant recompilé et le reflow forcé par accès géométrique.","tags":["javascript","layout-thrashing","loop-invariant","dom"],"optionNotes":["(1) new RegExp recompile l'invariant ; (2) lire offsetHeight après appendChild force un reflow (layout thrashing).","Faux : getElementById est appelé une fois hors boucle ; innerHTML n'est pas le vrai souci.","Faux : pas de GC par replace ni par appendChild.","Faux : offsetHeight n'est pas mis en cache ; sa lecture après mutation force justement le reflow."]},
  {"id":"perf-batching-reseau-latence-vs-debit-et-la-l","type":"mcq","domain":"software","topic":"performance","difficulty":"senior","title":"Batching réseau : latence vs débit et la loi de Little","company":"Little's Law (L = λW) ; Brendan Gregg, Systems Performance ch. 2 (throughput vs latency) ; Nagle's algorithm","question":"Un client envoie de nombreux petits messages réseau. Regrouper (batching) plusieurs messages par envoi augmente le débit mais dégrade une autre métrique. Quel est le compromis, illustré par l'algorithme de Nagle ?","options":["Le batching améliore le DÉBIT (moins d'overhead fixe par octet utile : en-têtes, syscalls, aller-retours) mais augmente la LATENCE des premiers messages, retenus jusqu'à ce que le lot se remplisse ou qu'un délai expire — exactement le compromis de l'algorithme de Nagle","Le batching améliore à la fois débit et latence sans compromis ; l'algorithme de Nagle est obsolète et ne devrait jamais être désactivé sur les connexions modernes","Le batching réduit le débit car les gros paquets saturent les buffers réseau et déclenchent des retransmissions ; Nagle compense en fragmentant les paquets pour éviter la congestion","Le batching augmente la latence ET réduit le débit, mais diminue l'usage CPU ; Nagle est un mécanisme purement CPU qui n'a aucun effet sur la latence réseau observée"],"answer":0,"explanation":"Compromis classique DÉBIT vs LATENCE. Regrouper de nombreux petits messages en un seul envoi AMORTIT les coûts fixes (en-têtes TCP/IP ~40 o par paquet, syscalls, allers-retours, traitement par paquet) sur davantage de données utiles → meilleur débit et efficacité. Mais les premiers messages du lot doivent ATTENDRE que le lot se remplisse (ou qu'un timer expire) avant d'être émis → leur latence augmente. L'algorithme de NAGLE illustre exactement cela : il retient les petits segments TCP jusqu'à recevoir l'ACK du précédent (ou accumuler un MSS), réduisant la profusion de minuscules paquets (le problème des 'tinygrams') au prix d'une latence accrue — d'où l'option TCP_NODELAY pour le désactiver quand la latence prime (jeux, RPC interactifs). Distracteurs : il y a bien un compromis (débit gagné, latence perdue), donc 'sans compromis' est faux ; le batching n'entraîne pas de retransmissions ni de baisse de débit (option 3, inverse) ; Nagle affecte directement la latence réseau observée (pas 'purement CPU'). Le principe : amortir le coût fixe augmente le débit mais peut retarder les unités individuelles.","tags":["batching","nagle","latence-debit","reseau"],"optionNotes":["Batcher amortit les coûts fixes (débit ↑) mais retient les premiers messages (latence ↑) : c'est Nagle.","Faux : il y a bien un compromis ; Nagle n'est pas obsolète (cf. TCP_NODELAY).","Faux : le batching n'entraîne pas de retransmissions ni de baisse de débit.","Faux : Nagle affecte directement la latence réseau, il n'est pas « purement CPU »."]},
];
